Entity Framework?
что это? (орм технология)
Entity Framework Core?
сущность?
отличетельная черта - linq

отношения между сущностями: внешние ключи, навигейшнпроперти
3 стратегии подгрузки данных (попробывать надо):
-Eager loading (жадная загрузка)
-Lazy loading
-Explicit loading

отношения:
-один к одному
-один ко многим
-многие ко многим

могут ли в энтити существовать/образщаться без ключа?
два типа организации связей?
собственные типы?
комплексные типы (в 8 версии)?

--
Наследование
ести 3 подхода организации маппинга:
-тэйбл иерархи (по умолчанию) tph, дескриминатор
-tpt - tabple per type, 
-ptc, (каждой модели отдельная таблица)

--
AsNoTracking?
запросы отслеживаемые и неотслеживаемые
ChangeTracker?

выполнение запросов, как они вообще обрабатываются?

IEnumerable и IQueryable?
как выбрать?
параллелизм? (напр редактирование одной и той же записи)
конкаренси токен?
конкренси чек?
таймстэмп?

