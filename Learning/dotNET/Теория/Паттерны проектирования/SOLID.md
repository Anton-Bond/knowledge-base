### **Принципы SOLID в .NET**

Принципы SOLID – это набор рекомендаций для проектирования гибкой, поддерживаемой и масштабируемой архитектуры в объектно-ориентированном программировании.

---

### **S – Single Responsibility Principle (Принцип единственной ответственности)**

Каждый класс должен иметь только одну причину для изменения.

❌ **Неправильно:**

```csharp
public class OrderService
{
    public void ProcessOrder(Order order) { /* обработка заказа */ }
    public void SaveToDatabase(Order order) { /* сохранение в БД */ }
    public void SendEmailConfirmation(Order order) { /* отправка email */ }
}
```

🔍 Здесь один класс отвечает за несколько задач: обработку заказа, сохранение в БД и отправку письма.

✅ **Правильно:**

```csharp
public class OrderProcessor
{
    public void Process(Order order) { /* обработка заказа */ }
}

public class OrderRepository
{
    public void Save(Order order) { /* сохранение заказа в БД */ }
}

public class EmailService
{
    public void SendConfirmation(Order order) { /* отправка письма */ }
}
```

📌 Теперь у каждого класса только одна причина для изменения.

---

### **O – Open/Closed Principle (Принцип открытости/закрытости)**

Код должен быть открыт для расширения, но закрыт для модификации.

❌ **Неправильно:**

```csharp
public class DiscountService
{
    public double ApplyDiscount(double price, string customerType)
    {
        if (customerType == "Regular") return price * 0.9;
        if (customerType == "VIP") return price * 0.8;
        return price;
    }
}
```

🔍 Если добавится новый тип клиента, придется менять код.

✅ **Правильно:**

```csharp
public interface IDiscountStrategy
{
    double ApplyDiscount(double price);
}

public class RegularCustomerDiscount : IDiscountStrategy
{
    public double ApplyDiscount(double price) => price * 0.9;
}

public class VIPCustomerDiscount : IDiscountStrategy
{
    public double ApplyDiscount(double price) => price * 0.8;
}

public class DiscountService
{
    private readonly IDiscountStrategy _discountStrategy;

    public DiscountService(IDiscountStrategy discountStrategy)
    {
        _discountStrategy = discountStrategy;
    }

    public double ApplyDiscount(double price) => _discountStrategy.ApplyDiscount(price);
}
```

📌 Теперь можно добавлять новые скидки, не изменяя код `DiscountService`.

---

### **L – Liskov Substitution Principle (Принцип подстановки Барбары Лисков)**

Дочерний класс должен заменять родительский без изменения логики.

❌ **Неправильно:**

```csharp
public class Bird
{
    public virtual void Fly() { Console.WriteLine("Птица летит"); }
}

public class Penguin : Bird
{
    public override void Fly() { throw new NotImplementedException(); }
}
```

🔍 `Penguin` – это тоже `Bird`, но не умеет летать. Нарушение принципа.

✅ **Правильно:**

```csharp
public abstract class Bird { }

public interface IFlyable
{
    void Fly();
}

public class Sparrow : Bird, IFlyable
{
    public void Fly() { Console.WriteLine("Воробей летит"); }
}

public class Penguin : Bird
{
    public void Swim() { Console.WriteLine("Пингвин плавает"); }
}
```

📌 Теперь `Penguin` не наследует ненужное поведение.

---

### **I – Interface Segregation Principle (Принцип разделения интерфейсов)**

Интерфейсы должны быть узкими и не перегруженными.

❌ **Неправильно:**

```csharp
public interface IWorker
{
    void Work();
    void Eat();
}

public class Robot : IWorker
{
    public void Work() { /* работает */ }
    public void Eat() { throw new NotImplementedException(); }
}
```

🔍 Робот не ест, но вынужден реализовывать метод `Eat()`.

✅ **Правильно:**

```csharp
public interface IWorkable
{
    void Work();
}

public interface IEatable
{
    void Eat();
}

public class Human : IWorkable, IEatable
{
    public void Work() { /* работает */ }
    public void Eat() { /* ест */ }
}

public class Robot : IWorkable
{
    public void Work() { /* работает */ }
}
```

📌 Теперь `Robot` реализует только нужный интерфейс.

---

### **D – Dependency Inversion Principle (Принцип инверсии зависимостей)**

Зависимости должны строиться на абстракциях, а не на конкретных реализациях.

❌ **Неправильно:**

```csharp
public class FileLogger
{
    public void Log(string message) { /* логирование в файл */ }
}

public class OrderService
{
    private FileLogger _logger = new FileLogger();

    public void ProcessOrder()
    {
        _logger.Log("Обрабатываем заказ");
    }
}
```

🔍 `OrderService` жёстко зависит от `FileLogger`, его трудно заменить.

✅ **Правильно:**

```csharp
public interface ILogger
{
    void Log(string message);
}

public class FileLogger : ILogger
{
    public void Log(string message) { /* логирование в файл */ }
}

public class ConsoleLogger : ILogger
{
    public void Log(string message) { Console.WriteLine(message); }
}

public class OrderService
{
    private readonly ILogger _logger;

    public OrderService(ILogger logger)
    {
        _logger = logger;
    }

    public void ProcessOrder()
    {
        _logger.Log("Обрабатываем заказ");
    }
}
```

📌 Теперь можно передавать любой логгер (`FileLogger`, `ConsoleLogger` и т. д.).

---

Зачем это нужно?
- Изменение одной части приложения ломает всё (нарушение **S**).
- Добавление новой фичи требует переписывать старый код (нарушение **O**).
- Наследники ведут себя непредсказуемо (нарушение **L**).
- Перегруженные интерфейсы заставляют писать лишний код (нарушение **I**).
- Зависимость от конкретных реализаций делает код негибким (нарушение **D**).

SOLID помогает строить архитектуру, которая выдерживает изменения и масштабируется без лилшних заморочек.
### **Вывод**

Принципы SOLID помогают писать чистый, поддерживаемый и масштабируемый код. Использование этих принципов делает архитектуру гибкой и предотвращает распространенные проблемы проектирования.

---
### **Пример простым языком**

Представь, что ты управляешь рестораном. Чтобы все работало гладко, у тебя есть персонал, кухня, поставщики и клиенты. Теперь давай разберем принципы SOLID на примере ресторана.

---

### **S – Принцип единственной ответственности (Single Responsibility Principle, SRP)**

📌 **Каждый отвечает только за свою работу.**

Представь, что у тебя есть один сотрудник, который готовит еду, принимает заказы, моет посуду и управляет финансами. Это хаос! Такой человек быстро запутается, и работа встанет.

✅ Вместо этого ты нанимаешь:

- Поваров – готовят еду.
- Официантов – принимают заказы.
- Бухгалтера – ведет финансы.
- Уборщика – поддерживает чистоту.

**Вывод:** если каждый делает только свою работу, ресторан работает эффективно. В программировании классы тоже должны заниматься чем-то одним.

---

### **O – Принцип открытости/закрытости (Open/Closed Principle, OCP)**

📌 **Можно добавить что-то новое, не ломая старое.**

Допустим, твой ресторан продает только пиццу. Но ты решил добавить в меню суши.

❌ **Неправильный подход:** ты переделываешь всю кухню, меняешь печи на рисоварки и увольняешь старых поваров.  
✅ **Правильный подход:** ты нанимаешь нового повара по суши и добавляешь в меню новый раздел.

**Вывод:** код должен позволять добавлять новое без необходимости менять старый код.

---

### **L – Принцип подстановки Барбары Лисков (Liskov Substitution Principle, LSP)**

📌 **Подмена сотрудников без сюрпризов.**

Представь, что ты нанял нового официанта, но он не умеет принимать заказы, а только убирать со столов. Клиенты ждут, а заказы не записываются.

❌ **Ошибка:** официант должен делать то же самое, что и его коллеги.  
✅ **Решение:** ты нанимаешь уборщика вместо неправильного официанта.

**Вывод:** если заменяем что-то новым, оно должно работать так же, как старое, без неожиданностей.

---

### **I – Принцип разделения интерфейсов (Interface Segregation Principle, ISP)**

📌 **Не давай всем сотрудникам лишнюю работу.**

Представь, что ты написал инструкцию для персонала:

> "Каждый работник должен готовить еду, убирать зал и обслуживать клиентов."

Официант не умеет готовить! Уборщик не должен принимать заказы!

✅ **Правильный подход:**

- Для поваров – инструкции по готовке.
- Для официантов – правила обслуживания.
- Для уборщиков – список зон для уборки.

**Вывод:** не надо заставлять кого-то делать то, что ему не нужно. В программировании интерфейсы (инструкции для кода) тоже должны быть маленькими и четкими.

---

### **D – Принцип инверсии зависимостей (Dependency Inversion Principle, DIP)**

📌 **Ресторан работает с разными поставщиками, а не зависит от одного.**

Допустим, твой ресторан закупает продукты только у одного поставщика. Вдруг он закрылся – ресторан не может работать.

✅ **Решение:** ты работаешь с несколькими поставщиками. Если один исчезнет, всегда найдется другой.

**Вывод:** в программировании программы должны зависеть от общих правил (абстракций), а не от конкретных вещей, чтобы легко менять детали.

---

### **Заключение**

Принципы SOLID помогают создать «умный» код, который легко расширять, поддерживать и понимать.

Если представить проект как ресторан, то следование этим принципам делает его управляемым, прибыльным и удобным для работы. 😊
