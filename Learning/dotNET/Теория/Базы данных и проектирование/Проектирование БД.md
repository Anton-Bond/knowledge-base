## **Проектирование баз данных**

### **1. Что такое проектирование БД?**

Проектирование базы данных — это **процесс создания структуры данных**, которая обеспечивает:  
✅ **Скалируемость** – База данных должна расти без потери производительности.  
✅ **Целостность данных** – Данные должны быть точными, непротиворечивыми.  
✅ **Производительность** – Запросы должны выполняться быстро.  
✅ **Гибкость** – Структура должна адаптироваться к изменениям.

---

## **2. Принципы проектирования базы данных**

### 📌 **2.1. Скалируемость**

✔ **Горизонтальное масштабирование** (шардирование) – Разделение БД на части.  
✔ **Вертикальное масштабирование** – Увеличение мощности сервера.  
✔ **Кеширование** (Redis, Memcached) – Снижение нагрузки на БД.

🔹 **Пример: как шардировать пользователей?**

- Разделить по первой букве логина (`A-M` → Сервер 1, `N-Z` → Сервер 2).
- Использовать **консистентный хеш** (на основе ID).

---

### 📌 **2.2. Целостность данных**

Целостность данных – это защита от ошибок.

**Типы целостности:**  
✅ **Сущностная целостность** – Каждая строка имеет **уникальный первичный ключ (PK)**.  
✅ **Ссылочная целостность** – Внешние ключи (FK) гарантируют, что данные не потеряются.  
✅ **Бизнес-логика** – Ограничения (CHECK, UNIQUE, NOT NULL).

🔹 **Пример: защита от удаления связанных данных**

```sql
ALTER TABLE Orders ADD CONSTRAINT FK_Customer FOREIGN KEY (CustomerId) 
REFERENCES Customers(Id) ON DELETE CASCADE;
```

📌 Теперь при удалении клиента удаляются и его заказы.

---

### 📌 **2.3. Производительность**

✔ **Индексы** – Ускоряют поиск (`CREATE INDEX`).  
✔ **Денормализация** – Дублирование данных для скорости.  
✔ **Партиционирование** – Разделение таблиц на части.  
✔ **Оптимизация запросов** – Использование `EXPLAIN ANALYZE` в PostgreSQL и `Execution Plan` в SQL Server.

🔹 **Пример: индекс ускоряет поиск по имени**

```sql
CREATE INDEX idx_customers_name ON Customers (Name);
```

📌 Теперь `SELECT * FROM Customers WHERE Name = 'Ivan'` быстрее.

---

### 📌 **2.4. Гибкость**

✔ База данных должна **адаптироваться к изменениям**.  
✔ Использовать **миграции** в EF Core.  
✔ Избегать жестких связей (иногда FK мешает изменять структуру).

---

## **3. Ошибки при проектировании базы данных**

### ❌ **3.1. Отсутствие нормализации**

Проблема: **Дублирование данных, сложность обновлений**.  
Пример **неправильной структуры (без нормализации):**

|OrderId|CustomerName|ProductName|
|---|---|---|
|1|Иван|Ноутбук|
|2|Иван|Мышь|

**Правильная структура (нормализация, разнесение на таблицы):**  
**Customers:**

|Id|Name|
|---|---|
|1|Иван|

**Orders:**

|Id|CustomerId|
|---|---|
|1|1|

**OrderItems:**

|OrderId|ProductName|
|---|---|
|1|Ноутбук|
|1|Мышь|

Теперь **данные не дублируются**.

---

### ❌ **3.2. Избыточно сложные схемы**

✔ Не нужно создавать **слишком много таблиц**.  
✔ Иногда лучше **денормализовать** (добавить дублирующиеся данные).

Пример **плохой схемы:**

- Таблица `Users` с 20+ связями.
- Таблица `Addresses` с отдельной таблицей `Cities`.

**Лучше** просто хранить `Address VARCHAR(255)`.

---

### ❌ **3.3. Проблемы с индексами**

✔ **Отсутствие индексов** → Медленные запросы.  
✔ **Избыток индексов** → Увеличение размера БД, замедление `INSERT/UPDATE/DELETE`.

Пример **ошибки** (избыток индексов):

```sql
CREATE INDEX idx_orders_customer ON Orders (CustomerId);
CREATE INDEX idx_orders_date ON Orders (OrderDate);
CREATE INDEX idx_orders_total ON Orders (TotalAmount);
```

📌 Лучше создать **композитный индекс**:

```sql
CREATE INDEX idx_orders ON Orders (CustomerId, OrderDate);
```

---

### ❌ **3.4. Отсутствие четких связей**

✔ Не хранить все в одной таблице (не делать "гигантскую таблицу").  
✔ **Использовать FK** там, где это необходимо.

Пример **ошибки (нет связи):**

```sql
CREATE TABLE Orders (
   Id INT PRIMARY KEY,
   CustomerName VARCHAR(255) -- Без связи!
);
```

📌 Правильный вариант:

```sql
CREATE TABLE Orders (
   Id INT PRIMARY KEY,
   CustomerId INT,
   FOREIGN KEY (CustomerId) REFERENCES Customers(Id)
);
```

---

## **4. Три формы нормализации**

### **📌 1-я нормальная форма (1NF) – Отсутствие повторяемых групп данных**

**Нельзя:** хранить **массив значений в одной колонке**.

❌ Плохой пример:

|Id|Name|PhoneNumbers|
|---|---|---|
|1|Иван|123, 456|

✔ **Правильный вариант (разделить на таблицы):**  
**Customers:**

|Id|Name|
|---|---|
|1|Иван|

**CustomerPhones:**

|Id|CustomerId|PhoneNumber|
|---|---|---|
|1|1|123|
|2|1|456|

---

### **📌 2-я нормальная форма (2NF) – Все колонки зависят от первичного ключа**

❌ Плохой пример (лишние данные):

|OrderId|CustomerId|CustomerName|
|---|---|---|
|1|1|Иван|

📌 `CustomerName` не зависит от `OrderId`.  
✔ **Правильный вариант:** `CustomerName` выносим в `Customers`.

---

### **📌 3-я нормальная форма (3NF) – Исключение зависимых данных**

❌ Плохой пример (город в `Customers` зависит от `ZipCode`):

|Id|Name|ZipCode|City|
|---|---|---|---|
|1|Иван|101000|Москва|

✔ **Правильный вариант:** `ZipCode → City` выносим в отдельную таблицу.

### **📌 4-я нормальная форма () – бойса кода, сторогая 3я (отсутствие зависимости на кондидата ключей) -- погуглить**

---

## **5. Денормализация таблицы**

Иногда **нормализация замедляет работу** (слишком много `JOIN`).

🔹 **Денормализация** – это **обратный процесс**:  
✔ Добавляем **дублирующиеся данные** для скорости.  
✔ Используется в **отчетах, аналитике**.

Пример **денормализации (добавление данных прямо в Orders)**:

```sql
ALTER TABLE Orders ADD COLUMN CustomerName VARCHAR(255);
```

Теперь можно делать `SELECT` без `JOIN`.

---

## **6. Управление сложностью**

Проектирование БД – это **поиск компромиссов** между:  
✔ Производительностью  
✔ Гибкостью  
✔ Целостностью

**"Из двух зол выбираем меньшее":**  
✅ Если важна **скорость** – допускаем **денормализацию**.  
✅ Если важна **целостность** – используем **FK и нормализацию**.

---

## **Вывод**

📌 **Хороший дизайн БД** → Быстрая, надежная, удобная.  
📌 **Плохой дизайн** → Медленная, сложная, ломается.

что необходимо сделать для оптимизации запроса:
 - использ джоин, причем иннер предпочтительне
 - индексы
 - не использа селект звездоска а только необход столбцы
 - если большая таблица - портиционирование, денормализация
