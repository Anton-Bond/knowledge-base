### **1. Что такое триггеры?**

**Триггер (Trigger)** — это специальный объект базы данных, который автоматически выполняется при наступлении определённого события (`INSERT`, `UPDATE`, `DELETE`) в таблице.

Триггеры чаще всего используются для:

- Поддержания целостности данных.
- Логирования изменений.
- Автоматического обновления других таблиц.

**Пример создания триггера (логирование изменений):**

```sql
CREATE TRIGGER trg_AfterUpdateCustomers  
ON Customers  
AFTER UPDATE  
AS  
BEGIN  
    INSERT INTO AuditLog (TableName, ActionType, ChangeDate)  
    VALUES ('Customers', 'UPDATE', GETDATE());  
END;
```

Этот триггер срабатывает **после обновления** данных в таблице `Customers` и записывает информацию в `AuditLog`.

---

### **2. Виды триггеров**

📌 **DML-триггеры (Data Manipulation Language)**  
Срабатывают при изменении данных в таблице (`INSERT`, `UPDATE`, `DELETE`).

📌 **DDL-триггеры (Data Definition Language)**  
Срабатывают при изменении структуры базы (`CREATE`, `ALTER`, `DROP`).

📌 **INSTEAD OF-триггеры**  
Позволяют **заменить стандартное выполнение операции**, например, вместо `DELETE` просто помечать запись как удалённую (`soft delete`).

**Пример INSTEAD OF DELETE:**

```sql
CREATE TRIGGER trg_SoftDeleteOrders  
ON Orders  
INSTEAD OF DELETE  
AS  
BEGIN  
    UPDATE Orders SET IsDeleted = 1 WHERE OrderID IN (SELECT OrderID FROM deleted);  
END;
```

Теперь при `DELETE` данные не удаляются, а просто помечаются как удалённые (`IsDeleted = 1`).

---

### **3. Основные проблемы триггеров**

⚠ **1. Скрытые операции**  
Триггеры выполняются **автоматически**, и разработчик может не знать, что после `INSERT` или `UPDATE` запускаются дополнительные действия.  
👉 Это усложняет отладку и поддержку.

⚠ **2. Замедляют работу БД**  
Каждый триггер выполняет дополнительный SQL-код, что увеличивает время выполнения `INSERT`, `UPDATE`, `DELETE`.  
👉 Особенно критично при массовых операциях (`BULK INSERT`).

⚠ **3. Вызов триггера внутри триггера (каскадность)**  
Если триггер `A` вызывает триггер `B`, а `B` вызывает `C`, можно получить **зацикливание** или неожиданную нагрузку на сервер.

⚠ **4. Трудности с отладкой**  
Триггеры не вызываются явно, их нельзя протестировать, как обычные процедуры.  
👉 Чтобы отследить выполнение, приходится логировать внутри триггера (`PRINT`, `INSERT INTO LogTable`).

⚠ **5. Повышенная сложность бизнес-логики**  
Если часть логики находится в коде C#, а другая — в триггерах, это усложняет поддержку и разработку.

⚠ **6. Неочевидное влияние на производительность**  
Например, если триггер на `UPDATE` выполняет сложный SQL-запрос, каждое обновление строки будет занимать **намного больше времени**.

---

### **Вывод**

✅ **Когда использовать триггеры:**

- Логирование изменений (`AuditLog`).
- Автоматическое обеспечение целостности данных.
- Реализация `soft delete` через `INSTEAD OF DELETE`.

❌ **Когда НЕ использовать триггеры:**

- Когда можно заменить их бизнес-логикой в C# или в хранимых процедурах.
- Когда триггеры замедляют массовые операции.
- Когда сложно контролировать порядок их выполнения.
