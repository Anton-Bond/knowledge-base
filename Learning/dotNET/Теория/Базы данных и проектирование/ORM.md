### **1. Что такое ORM?**

**ORM (Object-Relational Mapping)** — это технология, позволяющая работать с базой данных через объекты в коде, а не через SQL-запросы.

✅ Вместо написания SQL-запросов:

```sql
SELECT * FROM Customers WHERE Id = 1;
```

Можно использовать объектный подход:

```csharp
var customer = dbContext.Customers.Find(1);
```

---

### **2. Зачем нужен ORM?**

🔹 **Сокращает количество кода** — не нужно писать SQL вручную.  
🔹 **Абстрагирует от конкретной БД** — код одинаково работает с SQL Server, PostgreSQL, MySQL.  
🔹 **Облегчает поддержку и читаемость** — работа с данными похожа на обычные коллекции.  
🔹 **Упрощает работу с транзакциями, кэшированием, миграциями**.

---

### **3. ORM в .NET**

В .NET есть три популярных способа работы с базой данных:  
1️⃣ **Entity Framework Core (EF Core)** – мощный ORM с автоматической генерацией SQL.  
2️⃣ **Dapper** – лёгкий ORM для ручного управления SQL-запросами.  
3️⃣ **ADO.NET** – низкоуровневый доступ к БД без абстракций.

---

### **4. Entity Framework Core (EF Core)**

📌 **Особенности:**  
✔ **Лёгкий старт** – быстрое подключение и работа с данными.  
✔ **Миграции** – можно изменять схему БД из кода.  
✔ **Работа с LINQ** – SQL-запросы заменяются удобными LINQ-выражениями.  
✔ **Трекер изменений** – автоматически следит за изменёнными объектами.

📌 **Пример использования EF Core:**

```csharp
public class ApplicationDbContext : DbContext  
{  
    public DbSet<Customer> Customers { get; set; }  
}  

var customer = new Customer { Name = "John Doe" };  
dbContext.Customers.Add(customer);  
dbContext.SaveChanges();
```

📌 **Минусы:**  
❌ **Медленный в сложных запросах** – генерируемый SQL не всегда оптимален.  
❌ **Большой размер библиотеки** – накладывает нагрузку на приложение.  
❌ **Трудности с оптимизацией** – требует глубокого понимания (`AsNoTracking`, `Include`, `Projection`).

✅ **Когда использовать?**

- Когда нужен **быстрый старт**.
- Когда важна **поддержка миграций** и автоматическое отслеживание изменений.
- Когда большинство запросов **простые** (`CRUD`).

🚨 **"Easy to start, hard to expert"** – легко освоить, но сложно оптимизировать.

---

### **5. Dapper**

📌 **Особенности:**  
✔ **Быстрее EF Core** – работает ближе к "чистому" SQL.  
✔ **Гибкость** – полное управление SQL-запросами.  
✔ **Простая настройка** – всего один файл `.cs`.

📌 **Пример использования Dapper:**

```csharp
using (var connection = new SqlConnection(connectionString))  
{  
    var customer = connection.QueryFirstOrDefault<Customer>("SELECT * FROM Customers WHERE Id = @Id", new { Id = 1 });  
}
```

📌 **Минусы:**  
❌ **Нет трекинга изменений** – нужно обновлять данные вручную.  
❌ **Нет миграций** – схему БД приходится поддерживать отдельно.  
❌ **Больше кода** – приходится писать SQL-запросы самому.

✅ **Когда использовать?**

- Когда важна **производительность** (высоконагруженные запросы).
- Когда нужна **гибкость SQL** (оптимизированные запросы, сложные `JOIN`).
- Когда **не нужен трекинг изменений** (чтение данных, отчёты).

почитать про IDataReader

---

### **6. ADO.NET**

📌 **Особенности:**  
✔ **Прямой доступ к БД** – полное управление соединением и командами SQL.  
✔ **Максимальная производительность** – без лишних абстракций.  
✔ **Гибкость** – можно работать с любыми SQL-запросами.

📌 **Пример использования ADO.NET:**

```csharp
using (SqlConnection connection = new SqlConnection(connectionString))  
{  
    connection.Open();  
    SqlCommand command = new SqlCommand("SELECT * FROM Customers WHERE Id = @Id", connection);  
    command.Parameters.AddWithValue("@Id", 1);  
    SqlDataReader reader = command.ExecuteReader();  
    while (reader.Read())  
    {  
        Console.WriteLine(reader["Name"]);  
    }  
}
```

📌 **Минусы:**  
❌ **Много кода** – ручная работа с соединениями, транзакциями, обработкой ошибок.  
❌ **Сложность в поддержке** – код менее читаем, чем EF Core или Dapper.  
❌ **Нет встроенного трекинга изменений** – всё нужно делать вручную.

✅ **Когда использовать?**

- Когда важен **максимальный контроль** над SQL.
- Когда ORM слишком "тяжёлый" для задачи.
- Когда приложение уже использует ADO.NET и нужно **минимизировать изменения**.

---

### **7. Сравнительная таблица**

|Функция|**Entity Framework Core**|**Dapper**|**ADO.NET**|
|---|---|---|---|
|Скорость|❌ Медленный в сложных запросах|✅ Очень быстрый|✅ Максимально быстрый|
|Простота|✅ Лёгкий старт|⚖ Средний|❌ Сложный|
|Автоматическое создание SQL|✅ Да|❌ Нет (SQL вручную)|❌ Нет (SQL вручную)|
|Миграции БД|✅ Да|❌ Нет|❌ Нет|
|Гибкость|⚖ Средняя (ORM-абстракция)|✅ Высокая|✅ Полный контроль|
|Трекер изменений|✅ Да|❌ Нет|❌ Нет|
|Поддержка транзакций|✅ Да|✅ Да|✅ Да|

---

### **Вывод**

🔹 **Entity Framework Core** – подходит для **обычных CRUD-операций**, удобен для новичков, но сложен в оптимизации.  
🔹 **Dapper** – лучше, если нужна **гибкость SQL** и **высокая скорость**. Хорош для отчётов, сложных `JOIN`.  
🔹 **ADO.NET** – используется, если нужен **полный контроль** над SQL-запросами или работа с БД на низком уровне.
