# **1. Что такое уровень изоляции?**

Уровень изоляции определяет, **насколько одна транзакция видит изменения другой незавершенной транзакции**.

Чем **выше уровень изоляции**, тем **лучше защита данных**, но **хуже производительность**.

---

# **2. Проблемы конкурентного доступа**

Низкие уровни изоляции приводят к **аномалиям (неожиданным проблемам с данными):**

|Аномалия|Описание|Пример|
|---|---|---|
|**Грязное чтение (Dirty Read)**|Чтение **несохранённых (неподтверждённых)** изменений другой транзакции|Транзакция 1 изменяет баланс, но ещё не сделала `COMMIT`. Транзакция 2 читает **неподтвержденный** баланс, но Транзакция 1 делает `ROLLBACK`. В результате, Транзакция 2 получила **ложные данные**.|
|**Неповторяющееся чтение (Non-Repeatable Read)**|Один и тот же запрос в одной транзакции **возвращает разные результаты**|Транзакция 1 дважды читает баланс пользователя. Между чтениями Транзакция 2 обновила баланс. В результате, Транзакция 1 видит **разные значения**.|
|**Фантомное чтение (Phantom Read)**|Чтение **разного количества строк** при одинаковом запросе|Транзакция 1 делает `SELECT COUNT(*) FROM Users`. В этот момент Транзакция 2 добавляет новую запись и делает `COMMIT`. Если Транзакция 1 снова запустит запрос, она получит **другое количество строк**.|

---

# **3. Уровни изоляции в SQL Server**

SQL Server поддерживает 6 уровней изоляции (5 стандартных + 1 Snapshot).

|Уровень изоляции|Грязное чтение|Неповторяющееся чтение|Фантомное чтение|Блокировки|
|---|---|---|---|---|
|**Read Uncommitted**|❌ ДА|❌ ДА|❌ ДА|❌ Нет|
|**Read Committed (по умолчанию в SQL Server)**|✅ НЕТ|❌ ДА|❌ ДА|🔄 Чтение блокирует только изменяемые строки|
|**Repeatable Read**|✅ НЕТ|✅ НЕТ|❌ ДА|🔒 Блокирует изменённые и читаемые строки|
|**Serializable**|✅ НЕТ|✅ НЕТ|✅ НЕТ|🔒🔒 Блокирует диапазоны строк|
|**Snapshot (альтернативный)**|✅ НЕТ|✅ НЕТ|✅ НЕТ|🔄 Работает через **версионирование**|

---

# **4. Разбор уровней с примерами**

## **📌 4.1. Read Uncommitted (самый слабый)**

✔ **Максимальная скорость**, ❌ но транзакции могут видеть **грязные данные**.  
Используется, когда **неважна точность данных** (например, для аналитики).

🔹 **Пример проблемы (Грязное чтение)**

```sql
-- Транзакция 1 (изменение, но без COMMIT)
BEGIN TRANSACTION;
UPDATE Accounts SET Balance = Balance - 100 WHERE Id = 1;
-- (без COMMIT, данные еще не зафиксированы)

-- Транзакция 2 (читаем несохраненные данные)
SELECT Balance FROM Accounts WHERE Id = 1;
```

Транзакция 2 может получить **неподтвержденное значение** баланса!

🔹 **Решение:** использовать **Read Committed**.

---

## **📌 4.2. Read Committed (уровень по умолчанию)**

✔ **Грязные данные не читаются**, ❌ но возможны **неповторяющиеся и фантомные чтения**.

🔹 **Как работает:**

- Читает **только подтверждённые данные** (`COMMIT`).
- **Не блокирует другие `SELECT`**, но блокирует `UPDATE` и `DELETE`.

🔹 **Решение проблемы грязного чтения**

```sql
SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
BEGIN TRANSACTION;
SELECT Balance FROM Accounts WHERE Id = 1;
```

Теперь данные всегда **актуальные и подтвержденные**.

---

## **📌 4.3. Repeatable Read (сильнее, но медленнее)**

✔ **Гарантирует, что один и тот же запрос возвращает одинаковые данные**.  
❌ **Но фантомные записи возможны**.

🔹 **Блокирует** все строки, которые **читает или изменяет**.

🔹 **Пример решения проблемы неповторяющегося чтения:**

```sql
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
BEGIN TRANSACTION;
SELECT Balance FROM Accounts WHERE Id = 1;
-- Другое обновление заблокировано, пока текущая транзакция не завершится
```

---

## **📌 4.4. Serializable (самый строгий, но медленный)**

✔ **Полностью предотвращает фантомные чтения**.  
❌ **Создаёт блокировки на диапазоны строк**.

🔹 **Используется для критически важных операций** (финансы, учёт).  
🔹 **Пример решения проблемы фантомного чтения:**

```sql
SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;
BEGIN TRANSACTION;
SELECT COUNT(*) FROM Users;
-- Если другая транзакция попытается добавить нового пользователя, она заблокируется
```

Это **самый медленный** уровень, так как **блокирует целые диапазоны строк**.

---

## **📌 4.5. Snapshot (современная альтернатива)**

✔ **Не блокирует данные**, ❌ но увеличивает нагрузку на память.  
✔ Работает **через версионирование данных** (MVCC).

🔹 **Как включить в SQL Server:**

```sql
ALTER DATABASE MyDatabase SET ALLOW_SNAPSHOT_ISOLATION ON;
```

🔹 **Как использовать:**

```sql
SET TRANSACTION ISOLATION LEVEL SNAPSHOT;
BEGIN TRANSACTION;
SELECT * FROM Orders;
```

📌 **Преимущество:** параллельные транзакции работают быстрее, чем при `Serializable`.

---

# **5. Производительность уровней изоляции**

|Уровень|Скорость|Защита данных|Использование|
|---|---|---|---|
|**Read Uncommitted**|⚡ Самый быстрый|❌ Грязные данные|Аналитика, отчеты|
|**Read Committed**|🔄 Средняя|✅ Данные подтверждены|По умолчанию|
|**Repeatable Read**|🐢 Медленный|✅ Данные неизменны|Банковские транзакции|
|**Serializable**|🐢🐢 Самый медленный|✅ Полная изоляция|Критические операции|
|**Snapshot**|🚀 Быстро, но нагружает память|✅ Полная изоляция|Высоконагруженные системы|

---

# **Вывод**

📌 Если важна **максимальная производительность** → `Read Uncommitted` (но риск грязных данных).  
📌 Если нужна **гибкость и безопасность** → `Read Committed` (по умолчанию).  
📌 Если важна **консистентность данных** → `Repeatable Read` или `Serializable`.  
📌 Если нужны **быстрые параллельные запросы без блокировок** → `Snapshot`.
