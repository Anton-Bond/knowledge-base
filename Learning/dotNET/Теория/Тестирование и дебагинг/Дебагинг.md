## Дебагинг в .NET: Полное руководство

### 🔹 **Основные термины**

1. **Debugging** – процесс поиска и устранения ошибок в коде.
2. **Breakpoint** – точка останова, где выполнение кода приостанавливается для анализа.
3. **Watch** – отслеживание значений переменных во время выполнения.
4. **Call Stack** – стек вызовов, отображающий последовательность вызовов функций.
5. **Locals** – текущие локальные переменные и их значения.
6. **Threads** – управление потоками при отладке многопоточных приложений.
7. **Step Over (F10)** – выполнение текущей строки без захода внутрь вызванной функции.
8. **Step Into (F11)** – заход внутрь вызванной функции.
9. **Step Out (Shift+F11)** – выход из текущей функции.
10. **Attach to Process** – подключение к запущенному процессу для отладки.

---

### 🔹 **Основные аспекты**

1. **Отладка через Visual Studio**
    
    - Установка точек останова (Breakpoints).
    - Использование Conditional Breakpoints (условные точки останова).
    - Использование Data Breakpoints (останов по изменению переменной).
    - Анализ переменных через Locals и Watch.
    - Использование Immediate Window и Debug Console.
2. **Логирование и диагностика**
    
    - Использование `ILogger` в ASP.NET Core.
    - Запись логов через `Serilog`, `NLog`, `log4net`.
    - Инструменты диагностики `dotnet-trace`, `dotnet-counters`, `dotnet-dump`.
3. **Отладка многопоточных приложений**
    
    - Window → Threads в Visual Studio.
    - Использование `Parallel Stacks` для анализа потоков.
    - `Task` и `async/await`, их влияние на отладку.
4. **Дебагинг в Entity Framework Core**
    
    - Включение логирования SQL-запросов (`EnableSensitiveDataLogging`).
    - Использование `AsNoTracking` для тестирования запросов.
5. **Отладка ASP.NET Core**
    
    - Запуск с `dotnet watch` для быстрой отладки.
    - `Attach to Process` для отладки запущенного сервера.
    - Инструменты `Postman`, `Swagger` для тестирования API.
6. **Использование Remote Debugging**
    
    - Подключение к удаленному серверу через Visual Studio.
    - `dotnet-dbg` для отладки в Linux-контейнерах.

---

### 🔹 **Инструменты**

1. **Visual Studio Debugger** – основной инструмент отладки.
2. **dotnet-dump** – анализ дампов памяти.
3. **dotnet-trace** – трассировка выполнения кода.
4. **dotnet-counters** – мониторинг производительности .NET-приложений.
5. **WinDbg** – отладка на низком уровне.
6. **PerfView** – анализ производительности.
7. **Rider Debugger** – отладка в JetBrains Rider.

---

### 🔹 **Плюсы и минусы дебагинга**

✅ **Плюсы**:

- Позволяет находить ошибки в коде.
- Упрощает анализ работы приложения.
- Помогает в оптимизации производительности.

❌ **Минусы**:

- Может замедлять выполнение кода.
- Не заменяет правильное тестирование.
- Требует знаний работы инструментов отладки.

---

## **Diagnostics Tools в Visual Studio**

**Diagnostics Tools** – это встроенный инструмент Visual Studio для мониторинга производительности, анализа памяти и диагностики приложений во время отладки.

---

### 🔹 **Как открыть Diagnostics Tools?**

1. **Запустить отладку** (`F5` или `Ctrl+F5`).
2. **Открыть окно "Diagnostics Tools"**:
    - Меню **Debug → Windows → Show Diagnostic Tools**
    - Или нажать `Ctrl+Alt+F2`.

Если окно не отображается, убедитесь, что включен **Managed (Native) Debugging** в настройках проекта.

---

### 🔹 **Компоненты Diagnostics Tools**

После запуска окно состоит из трех основных разделов:

### **1. Events (События)**

Отображает ключевые события во время выполнения, включая:  
✅ **Breakpoints** – когда код останавливается на точке останова.  
✅ **Exceptions** – возникшие исключения.  
✅ **Thread switches** – переключение потоков в многопоточном коде.

📌 **Как использовать?**

- Можно фильтровать события по типу (исключения, точки останова и т. д.).
- Позволяет быстро анализировать причину сбоя.

---

### **2. CPU Usage (Использование процессора)**

Показывает загрузку процессора в реальном времени.

📌 **Как использовать?**

- Можно выявить, какие части кода потребляют больше ресурсов.
- Используется для анализа производительности (CPU профилирование).
- Доступен в режиме **Debug (F5)** и **Performance Profiler**.

---

### **3. Memory Usage (Использование памяти)**

Позволяет анализировать потребление памяти.

📌 **Как использовать?**

1. Нажать **Take Snapshot** – создаст снимок текущего состояния памяти.
2. Сравнить снимки для выявления утечек памяти.
3. Использовать **Garbage Collection (GC)** для анализа освобожденной памяти.

🔍 **Полезно для**:

- Отладки утечек памяти (`Memory Leak`).
- Оптимизации потребления памяти.
- Анализа `Garbage Collector` в .NET.

---

### **Дополнительные возможности**

✅ **Threads (Потоки)** – отслеживание активности потоков в многопоточном коде.  
✅ **Network (Сеть)** – мониторинг HTTP-запросов (ASP.NET Core).  
✅ **Exception Settings** – настройка реакции на исключения.

---

### 🔹 **Как использовать на практике?**

1️⃣ **Оптимизация CPU** – анализировать график CPU и находить узкие места.  
2️⃣ **Поиск утечек памяти** – делать **Memory Snapshot**, чтобы выявить рост памяти.  
3️⃣ **Диагностика исключений** – включить отображение событий и смотреть, где возникают ошибки.

**Diagnostics Tools – мощный инструмент для анализа производительности и отладки проблем!** 🚀  

---

### **Conditional Expressions** – это **условные выражения для точек останова (Conditional Breakpoints)**.

### 🔹 **Что это такое?**

Это точки останова, которые срабатывают **только при выполнении определенного условия**. Например, если переменная `x` равна 10, то выполнение кода остановится именно в этот момент.

### 🔹 **Как использовать в Visual Studio?**

1. **Поставить обычный Breakpoint** – кликнуть слева от строки кода.
2. **ПКМ → Conditions…**
3. В разделе **Condition** ввести условие, например:
    
    ```csharp
    x == 10
    ```
    
4. Нажать **Close**. Теперь код остановится только когда `x == 10`.

### 🔹 **Типы условий:**

1. **Expression (Выражение)** – точка останова срабатывает, если выражение `true`:
    
    ```csharp
    counter > 100
    ```
    
2. **Hit Count (Счетчик срабатываний)** – код остановится после `N` раз выполнения:
    - `>= 5` → останов после 5 выполнений.
3. **Filter (Фильтр по потокам, пользователям и пр.)**
    - Например, `ThreadId == 3` – остановится, если выполняется в потоке 3.

---

### 🔹 **Где это полезно?**

✅ Когда нужно отловить баг при определенных значениях.  
✅ При отладке циклов – можно остановиться на нужной итерации.  
✅ В многопоточных приложениях – можно отлаживать код только в нужном потоке.

---
---
### 🔹 **Remote Debugging (Удаленная отладка) в .NET**

**Remote Debugging** – это процесс отладки приложения, запущенного на удаленном сервере или в контейнере, с помощью отладчика на локальном компьютере.

---

### 🔹 **Когда это нужно?**

✅ Приложение развернуто на удаленном сервере и нельзя запустить его локально.  
✅ Отладка внутри **Docker-контейнера**.  
✅ Отладка на реальных боевых серверах (production debugging).  
✅ Анализ сложных багов, которые не воспроизводятся локально.

---

### 🔹 **Как настроить Remote Debugging в .NET?**

#### 1️⃣ **Удаленная отладка через Visual Studio (Windows, Linux, Docker)**

1. **Установить Remote Debugger** на сервере (можно скачать [Remote Tools for Visual Studio](https://visualstudio.microsoft.com/downloads/)).
2. Запустить `msvsmon.exe` на сервере (если Windows).
3. В Visual Studio:
    - `Debug` → `Attach to Process`
    - Указать IP-адрес сервера и процесс для отладки.
4. Установить breakpoints и запустить отладку! 🚀

#### 2️⃣ **Отладка ASP.NET Core в Docker**

1. Запуск контейнера с отладочными портами:
    
    ```sh
    docker run -e DOTNET_USE_POLLING_FILE_WATCHER=1 -p 5005:5005 myapp
    ```
    
2. В `launch.json` (для VS Code) настроить attach к контейнеру.
3. Подключиться к `dotnet`-процессу внутри контейнера.

#### 3️⃣ **Отладка с помощью SSH (для Linux-серверов)**

- Можно подключиться через `SSH Tunnel` и использовать `dotnet-trace`, `dotnet-dump` или `dotnet-counters`.
- Альтернатива – отладка через **JetBrains Rider** (есть встроенная поддержка SSH).

---

### 🔹 **Инструменты для Remote Debugging**

🛠 **Visual Studio Remote Debugging** – основной инструмент отладки через сеть.  
🛠 **dotnet-dump** – анализ дампа памяти удаленного процесса.  
🛠 **dotnet-trace** – сборка трассировок выполнения кода.  
🛠 **WinDbg** – мощная отладка на низком уровне.  
🛠 **JetBrains Rider Remote Debugger** – удобная отладка через SSH.

---

### 🔹 **Плюсы и минусы Remote Debugging**

✅ **Плюсы:**

- Позволяет отлаживать код прямо на удаленном сервере.
- Не нужно копировать проект на локальную машину.
- Можно анализировать сложные баги, которые не воспроизводятся локально.

❌ **Минусы:**

- Может замедлять работу сервера (нагрузка на CPU/RAM).
- Требует настройки сети и безопасности.
- В некоторых случаях проще использовать **логирование** (`ILogger`, `Serilog`).
