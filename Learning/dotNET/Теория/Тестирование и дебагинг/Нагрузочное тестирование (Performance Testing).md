Нагрузочное тестирование (Performance Testing) оценивает, как система работает под разными уровнями нагрузки, выявляет узкие места и определяет максимальную производительность приложения.

---

## **1️⃣ Виды нагрузочного тестирования**

🔹 **Load Testing (Тестирование нагрузки)**

- Оценивает, как система работает при ожидаемой нагрузке.
- Измеряет время отклика, потребление ресурсов, стабильность.

 **Популярные инструменты:**

| Инструмент    | Описание                                                              |
| ------------- | --------------------------------------------------------------------- |
| **k6**        | Легковесный инструмент для API-тестирования, поддерживает JavaScript. |
| **JMeter**    | Гибкий инструмент для HTTP, SOAP, REST API, DB-тестирования.          |
| **NBomber**   | Оптимизирован для .NET, позволяет писать тесты на C#.                 |
| **Artillery** | Прост в использовании, подходит для тестирования API.                 |

🔹 **Stress Testing (Стресс-тестирование)**

- Проверяет, как система ведёт себя при экстремальных нагрузках.
- Позволяет определить точку отказа системы.

**Популярные инструменты:**

| Инструмент  | Описание                                                              |
| ----------- | --------------------------------------------------------------------- |
| **k6**      | Поддерживает сценарии с резким увеличением нагрузки.                  |
| **JMeter**  | Позволяет задавать нагрузку выше нормального предела.                 |
| **Locust**  | Хорош для стресс-тестирования API, имитирует поведение пользователей. |
| **Gatling** | Поддерживает нагрузочные тесты на основе сценариев.                   |

🔹 **Spike Testing (Тестирование всплесков нагрузки)**

- Проверяет систему при резких скачках нагрузки.
- Полезно для тестирования сайтов во время акций, распродаж и т. д.

**Популярные инструменты:**

| Инструмент  | Описание                                                             |
| ----------- | -------------------------------------------------------------------- |
| **k6**      | Поддерживает этапы с резкими скачками нагрузки.                      |
| **NBomber** | Позволяет создавать сложные сценарии с неожиданными пиками нагрузки. |
| **JMeter**  | Генерирует нагрузку с быстрыми изменениями количества пользователей. |

🔹 **Soak Testing (Долговременное тестирование, или тест на истощение)**

- Имитация высокой нагрузки в течение длительного времени.
- Позволяет выявить утечки памяти, деградацию производительности.

**Популярные инструменты:**

| Инструмент  | Описание                                                        |
| ----------- | --------------------------------------------------------------- |
| **JMeter**  | Хорошо подходит для долгосрочных тестов с высокой нагрузкой.    |
| **k6**      | Поддерживает длительные сценарии без потери производительности. |
| **NBomber** | Позволяет имитировать нагрузку в течение часов или дней.        |

🔹 **Scalability Testing (Тестирование масштабируемости)**

- Проверяет, как система масштабируется при увеличении нагрузки.
- Позволяет оценить, как работает горизонтальное и вертикальное масштабирование.

**Популярные инструменты:**

| Инструмент                    | Описание                                                       |
| ----------------------------- | -------------------------------------------------------------- |
| **Azure Load Testing**        | Облачный инструмент для проверки масштабируемости.             |
| **k6 + Kubernetes**           | Используется для тестирования горизонтального масштабирования. |
| **JMeter (Distributed Mode)** | Запускает тесты на нескольких машинах.                         |

---

## **2️⃣ Инструменты для нагрузочного тестирования в .NET**

|Инструмент|Описание|
|---|---|
|**JMeter**|Один из самых популярных инструментов для тестирования производительности API и веб-приложений.|
|**k6**|Современный инструмент для нагрузочного тестирования с поддержкой JavaScript.|
|**Artillery**|Прост в использовании, подходит для тестирования API.|
|**Gatling**|Использует Scala, позволяет писать мощные сценарии нагрузки.|
|**Azure Load Testing**|Облачный сервис для нагрузочного тестирования в Azure.|
|**NBomber**|Кроссплатформенный инструмент для тестирования в .NET с поддержкой C#.|
|**Locust**|Лёгкий инструмент на Python, но его можно использовать с .NET API.|

---

## **3️⃣ Пример нагрузочного теста в .NET с k6**

📌 **k6** — это инструмент, позволяющий писать сценарии тестов на JavaScript и запускать их для проверки производительности API.

### **🔹 Установка k6**

```sh
choco install k6  # Windows (Chocolatey)
brew install k6   # macOS (Homebrew)
```

### **🔹 Пример теста API на k6**

Создадим файл `loadtest.js`:

```js
import http from 'k6/http';
import { check, sleep } from 'k6';

export let options = {
  stages: [
    { duration: '30s', target: 50 },  // Разогрев: 50 пользователей за 30 сек
    { duration: '1m', target: 100 },  // Основная нагрузка: 100 пользователей за 1 мин
    { duration: '30s', target: 0 }   // Завершение теста
  ],
};

export default function () {
  let res = http.get('https://yourapi.com/api/products');
  check(res, {
    'status is 200': (r) => r.status === 200,
    'response time < 500ms': (r) => r.timings.duration < 500,
  });
  sleep(1);
}
```

### **🔹 Запуск теста**

```sh
k6 run loadtest.js
```

🔹 После выполнения теста будет выведен отчёт со временем отклика, ошибками и метриками.

---

## **4️⃣ Нагрузочное тестирование ASP.NET Core с NBomber**

📌 **NBomber** — мощный инструмент, написанный на C#.

### **🔹 Установка NBomber**

Добавляем пакет:

```sh
dotnet add package NBomber
```

### **🔹 Пример теста API**

```csharp
using NBomber.CSharp;
using NBomber.Http.CSharp;

var step = Step.Create("fetch_products", async context =>
{
    using var httpClient = new HttpClient();
    var response = await httpClient.GetAsync("https://yourapi.com/api/products");

    return response.IsSuccessStatusCode
        ? Response.Ok()
        : Response.Fail();
});

var scenario = ScenarioBuilder.CreateScenario("load_test", step)
    .WithLoadSimulations(Simulation.Inject(rate: 50, interval: TimeSpan.FromSeconds(1), during: TimeSpan.FromMinutes(1)));

NBomberRunner
    .RegisterScenarios(scenario)
    .Run();
```

🔹 NBomber позволяет гибко управлять сценарием нагрузки и получать отчёты.

---

## **5️⃣ Метрики, которые нужно анализировать**

🔸 **Response Time (Время ответа)** – среднее и максимальное время отклика API.  
🔸 **Error Rate (Процент ошибок)** – процент неудачных запросов.  
🔸 **Requests per Second (RPS)** – сколько запросов обрабатывается в секунду.  
🔸 **CPU и RAM Usage** – насколько нагружен сервер.  
🔸 **Throughput (Пропускная способность)** – сколько данных передаётся в секунду.

---

## **6️⃣ Полезные ссылки**

🔗 [Официальный сайт k6](https://k6.io/)  
🔗 [Документация JMeter](https://jmeter.apache.org/)  
🔗 [NBomber для .NET](https://nbomber.com/)  
🔗 [Azure Load Testing](https://learn.microsoft.com/en-us/azure/load-testing/)
