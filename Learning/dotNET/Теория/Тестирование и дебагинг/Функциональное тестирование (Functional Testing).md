Функциональное тестирование (Functional Testing) проверяет, выполняет ли система или ее компоненты ожидаемое поведение согласно требованиям.

### **1️⃣ Основные характеристики функционального тестирования**

✅ Ориентировано на **проверку бизнес-логики**.  
✅ Фокусируется на **входных и выходных данных** (не на внутренней реализации).  
✅ Может быть **ручным или автоматизированным**.  
✅ Использует реальные или тестовые данные для проверки работы системы.

---

## **2️⃣ Виды функционального тестирования**

🔹 **Smoke Testing (дымовое тестирование)**

- Быстро проверяет, работает ли приложение после сборки.
- Например, проверка, запускается ли приложение и доступны ли основные страницы.

🔹 **Sanity Testing (санитарное тестирование)**

- Проверяет исправленные ошибки без полного регрессионного тестирования.
- Например, если исправлена ошибка в корзине интернет-магазина, проверяется, работает ли она теперь корректно.

🔹 **Regression Testing (регрессионное тестирование)**

- Проверяет, не сломались ли старые функции после внесения изменений.
- Может выполняться вручную или автоматически.

🔹 **End-to-End Testing (сквозное тестирование)**

- Проверяет полное взаимодействие пользователя с системой (от входа в систему до выполнения ключевых операций).
- Например, сценарий: **регистрация → авторизация → заказ товара → оплата**.

🔹 **Acceptance Testing (приемочное тестирование)**

- Проверяет, соответствует ли система бизнес-требованиям заказчика.
- Например, тестирование API на соответствие OpenAPI (Swagger).

---

## **3️⃣ Инструменты для функционального тестирования в .NET**

### **📌 Автоматизированное тестирование API**

|Инструмент|Описание|
|---|---|
|**RestSharp**|Библиотека для работы с HTTP-запросами, удобна для API-тестов.|
|**FluentAssertions**|Позволяет писать удобочитаемые ассерты.|
|**xUnit + TestServer**|Используется для интеграционных тестов ASP.NET Core API.|
|**Postman + Newman**|API-тестирование с возможностью автоматизации в CI/CD.|

### **📌 Автоматизированное тестирование UI**

| Инструмент     | Описание                                                                           |
| -------------- | ---------------------------------------------------------------------------------- |
| **Selenium**   | Позволяет тестировать UI веб-приложений.                                           |
| **Playwright** | Более современный инструмент для UI-тестирования, поддерживает параллельные тесты. |
| **Cypress**    | Легко интегрируется с CI/CD, подходит для тестирования веб-приложений.             |
| **SpecFlow**   | BDD-фреймворк для написания тестов в формате Gherkin.                              |

---

## **4️⃣ Пример теста API в .NET**

Используем **xUnit, FluentAssertions и RestSharp**:

```csharp
using System.Net;
using System.Threading.Tasks;
using FluentAssertions;
using RestSharp;
using Xunit;

public class ApiTests
{
    private readonly RestClient _client = new RestClient("https://jsonplaceholder.typicode.com");

    [Fact]
    public async Task Get_Posts_Should_Return_Valid_Response()
    {
        // Arrange
        var request = new RestRequest("/posts", Method.Get);

        // Act
        var response = await _client.ExecuteAsync(request);

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.OK);
        response.Content.Should().Contain("userId");
    }
}
```

🔹 **Что делает этот тест?**  
✔️ Делает GET-запрос к `https://jsonplaceholder.typicode.com/posts`.  
✔️ Проверяет, что статус ответа **200 OK**.  
✔️ Проверяет, что в ответе есть поле `"userId"`.

---

## **5️⃣ Пример теста UI в .NET (Selenium + xUnit)**

```csharp
using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using System;
using Xunit;

public class GoogleSearchTests : IDisposable
{
    private readonly IWebDriver _driver;

    public GoogleSearchTests()
    {
        _driver = new ChromeDriver();
    }

    [Fact]
    public void GoogleSearch_Should_Show_Results()
    {
        // Arrange
        _driver.Navigate().GoToUrl("https://www.google.com");
        var searchBox = _driver.FindElement(By.Name("q"));

        // Act
        searchBox.SendKeys("Selenium WebDriver");
        searchBox.Submit();

        // Assert
        Assert.Contains("Selenium WebDriver", _driver.Title);
    }

    public void Dispose()
    {
        _driver.Quit();
    }
}
```

🔹 **Что делает этот тест?**  
✔️ Открывает браузер **Chrome**.  
✔️ Переходит на `https://www.google.com`.  
✔️ Вводит "Selenium WebDriver" в поисковую строку и нажимает Enter.  
✔️ Проверяет, что в заголовке страницы есть "Selenium WebDriver".

---

## **6️⃣ Интеграция с CI/CD**

🔹 **GitHub Actions:**

- Можно автоматически запускать тесты API и UI при каждом коммите.
- Добавляется файл `.github/workflows/test.yml`:

```yaml
name: Run Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0'
      - name: Run tests
        run: dotnet test
```

🔹 **Azure DevOps:**

- Можно создать тестовый pipeline для запуска UI и API тестов.

---

## **7️⃣ Плюсы и минусы функционального тестирования**

### ✅ **Плюсы**

✔️ Выявляет критические ошибки в бизнес-логике.  
✔️ Может автоматизироваться, что снижает затраты времени.  
✔️ Улучшает пользовательский опыт (особенно UI-тесты).  
✔️ Помогает предотвратить регрессии.

### ❌ **Минусы**

⚠️ Может быть **медленным**, особенно UI-тестирование.  
⚠️ Требует **поддержки тестов**, так как UI может меняться.  
⚠️ API- и UI-тесты сложнее в отладке, чем юнит-тесты.  
⚠️ Некоторые сценарии сложны для автоматизации (например, капча, OTP-коды).

---

### 🚀 **Вывод**

Функциональное тестирование в .NET помогает выявлять ошибки на ранних этапах и гарантирует, что система работает согласно бизнес-требованиям. Для этого используются такие инструменты, как **RestSharp, Selenium, Playwright, xUnit и FluentAssertions**. Их можно интегрировать в **CI/CD**, автоматизируя тестирование API и UI.
