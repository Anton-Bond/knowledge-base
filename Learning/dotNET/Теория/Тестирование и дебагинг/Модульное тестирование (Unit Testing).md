Модульное тестирование (Unit Testing) проверяет работу отдельных модулей (методов, классов) в изоляции от остальных компонентов. Это основа автоматического тестирования, позволяющая найти ошибки на ранних этапах разработки.

---

## **1️⃣ Основные принципы модульного тестирования**

✅ **Изолированность** – каждый тест должен проверять только один метод или небольшую часть логики.  
✅ **Детерминированность** – результат теста должен быть одинаковым при каждом запуске.  
✅ **Автономность** – тест не должен зависеть от других тестов или внешних ресурсов (БД, файловой системы, API).  
✅ **AAA (Arrange – Act – Assert)** – стандартная структура тестов:

- **Arrange (Подготовка):** настройка тестовых данных и зависимостей.
- **Act (Действие):** вызов тестируемого метода.
- **Assert (Проверка):** сравнение ожидаемого и фактического результата.

---

## **2️⃣ Фреймворки для модульного тестирования**

|Фреймворк|Описание|
|---|---|
|**xUnit**|Современный, рекомендуется Microsoft, поддерживает Data-Driven Testing.|
|**NUnit**|Гибкий, удобный, поддерживает параметризованные тесты.|
|**MSTest**|Встроенный в Visual Studio, подходит для корпоративных проектов.|

---

## **3️⃣ Пример модульного теста с xUnit**

📌 **Пример: Тестирование метода сложения чисел**

📍 **Код тестируемого класса:**

```csharp
public class Calculator
{
    public int Add(int a, int b) => a + b;
}
```

📍 **Код теста с xUnit:**

```csharp
using Xunit;

public class CalculatorTests
{
    [Fact] // Атрибут Fact указывает, что это тестовый метод
    public void Add_ShouldReturnCorrectSum()
    {
        // Arrange (Подготовка)
        var calculator = new Calculator();
        
        // Act (Действие)
        var result = calculator.Add(2, 3);
        
        // Assert (Проверка)
        Assert.Equal(5, result);
    }
}
```

---

## **4️⃣ Использование Mock-объектов (имитация зависимостей)**

Часто тестируемые классы зависят от внешних сервисов (БД, API). Чтобы избежать реальных вызовов, используют **Mock-объекты** с помощью библиотек **Moq, FakeItEasy, NSubstitute**.

📌 **Пример: Тестирование сервиса с использованием Moq**

📍 **Сервис с зависимостью:**

```csharp
public interface IUserRepository
{
    User GetUserById(int id);
}

public class UserService
{
    private readonly IUserRepository _userRepository;

    public UserService(IUserRepository userRepository)
    {
        _userRepository = userRepository;
    }

    public string GetUserName(int id)
    {
        var user = _userRepository.GetUserById(id);
        return user != null ? user.Name : "Unknown";
    }
}
```

📍 **Тест с Moq:**

```csharp
using Moq;
using Xunit;

public class UserServiceTests
{
    [Fact]
    public void GetUserName_ShouldReturnUserName()
    {
        // Arrange
        var mockRepo = new Mock<IUserRepository>();
        mockRepo.Setup(repo => repo.GetUserById(1))
                .Returns(new User { Id = 1, Name = "Alice" });

        var service = new UserService(mockRepo.Object);

        // Act
        var result = service.GetUserName(1);

        // Assert
        Assert.Equal("Alice", result);
    }
}
```

### **🛠️ Разбор кода:**

🔹 `Mock<IUserRepository>` – создаёт мок-объект для `IUserRepository`.  
🔹 `Setup(repo => repo.GetUserById(1))` – указывает, что при вызове `GetUserById(1)` должен возвращаться объект `User`.  
🔹 `mockRepo.Object` – передаётся в `UserService` вместо реального `IUserRepository`.

---

## **5️⃣ Атрибуты для тестирования**

|Атрибут|Описание|
|---|---|
|`[Fact]`|Запускает обычный тест.|
|`[Theory]`|Позволяет передавать несколько значений в тест.|
|`[InlineData(2, 3, 5)]`|Передаёт параметры в тест (используется с `[Theory]`).|

📌 **Пример параметризованного теста:**

```csharp
[Theory]
[InlineData(2, 3, 5)]
[InlineData(-1, 4, 3)]
[InlineData(0, 0, 0)]
public void Add_ShouldReturnCorrectSum(int a, int b, int expected)
{
    var calculator = new Calculator();
    var result = calculator.Add(a, b);
    Assert.Equal(expected, result);
}
```

---

## **6️⃣ Организация тестов**

📌 **Лучшие практики:**  
✔️ Разделять тестируемый код и тесты (например, `MyApp.Tests`).  
✔️ Группировать тесты по классам (`UserServiceTests`, `CalculatorTests`).  
✔️ Использовать `FluentAssertions` для читаемых проверок.

📌 **Структура проекта:**

```
MyApp/
├── MyApp.Core/         # Основной код
├── MyApp.Tests/        # Тестовый проект
│   ├── CalculatorTests.cs
│   ├── UserServiceTests.cs
│   ├── TestDatabaseSetup.cs
```

---

## **7️⃣ Code Coverage (Покрытие кода тестами)**

Code Coverage показывает, какой процент кода покрыт тестами. В .NET можно использовать:  
✅ Встроенный инструмент в Visual Studio  
✅ **Coverlet** – расширение для .NET CLI  
✅ **ReportGenerator** – для генерации красивых отчётов

📌 **Пример запуска покрытия тестами через CLI:**

```sh
dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=lcov
```

---

## **8️⃣ Ошибки и антипаттерны**

⚠️ **Тестирование нескольких сценариев в одном тесте**  
❌ **Плохо:**

```csharp
[Fact]
public void Add_ShouldWork()
{
    var calculator = new Calculator();
    Assert.Equal(5, calculator.Add(2, 3));
    Assert.Equal(0, calculator.Add(0, 0));
    Assert.Equal(-1, calculator.Add(-1, 0));
}
```

✔️ **Хорошо (разделение тестов):**

```csharp
[Fact]
public void Add_ShouldReturnSum_WhenPositiveNumbers() { ... }

[Fact]
public void Add_ShouldReturnZero_WhenBothNumbersAreZero() { ... }
```

⚠️ **Зависимость тестов от внешних ресурсов (БД, файлы, API)**  
✅ Используйте Mock-объекты или SQLite in-memory для тестов.

⚠️ **Использование `Thread.Sleep` в тестах**  
✅ Вместо этого используйте `Task.Delay` или специальные инструменты для тестирования асинхронного кода.

---

## **🎯 Итог**

**Модульное тестирование** – это фундаментальная практика, которая:  
✅ Улучшает качество кода  
✅ Упрощает рефакторинг  
✅ Позволяет находить ошибки на ранних этапах

Для эффективного тестирования в .NET:

- Используйте **xUnit + Moq**
- Следуйте принципу **AAA**
- Разделяйте тесты и мокируйте зависимости

---
### **📌 NUnit vs xUnit vs MSTest — в чем разница? Как выбрать?**

Модульное тестирование в .NET чаще всего выполняется с помощью **xUnit, NUnit или MSTest**. Каждый фреймворк имеет свои особенности, плюсы и минусы. Давай разберёмся!

---

## **1️⃣ Краткое сравнение NUnit, xUnit и MSTest**

|Фреймворк|Поддержка .NET|Атрибуты тестов|Поддержка DI|Асинхронность|Развитие|Используется Microsoft|
|---|---|---|---|---|---|---|
|**xUnit**|✅ .NET Core / .NET 6+|`[Fact]`, `[Theory]`|✅ Да|✅ Да|🔥 Современный, активно развивается|✅ Да|
|**NUnit**|✅ .NET Core / .NET 6+|`[Test]`, `[TestCase]`|❌ Нет (но есть workarounds)|✅ Да|🟡 Развивается медленнее, но стабилен|❌ Нет|
|**MSTest**|✅ .NET Core / .NET 6+|`[TestMethod]`, `[DataTestMethod]`|✅ Да|✅ Да|🟡 Поддерживается Microsoft, но менее популярен|✅ Да|

### **🛠 Выбор фреймворка: когда использовать?**

|Сценарий|Рекомендуемый фреймворк|
|---|---|
|**Современные .NET проекты**|🔥 **xUnit** (рекомендуется Microsoft)|
|**Легаси-код, старые проекты**|🔹 **NUnit**|
|**Корпоративные проекты (MS-экосистема)**|🔹 **MSTest**|

---

## **2️⃣ NUnit vs xUnit: Основные отличия**

### **✅ Атрибуты тестов**

|Действие|xUnit|NUnit|
|---|---|---|
|Определение теста|`[Fact]`|`[Test]`|
|Параметризованный тест|`[Theory]` + `[InlineData]`|`[TestCase]`|
|Установка/очистка перед тестами|`IClassFixture<T>`|`[SetUp]`, `[TearDown]`|
|Группировка тестов|`CollectionFixture`|`[TestFixture]`|

📌 **Пример теста в xUnit:**

```csharp
public class CalculatorTests
{
    [Fact]
    public void Add_ShouldReturnSum()
    {
        var calc = new Calculator();
        var result = calc.Add(2, 3);
        Assert.Equal(5, result);
    }
}
```

📌 **Пример теста в NUnit:**

```csharp
[TestFixture]
public class CalculatorTests
{
    [Test]
    public void Add_ShouldReturnSum()
    {
        var calc = new Calculator();
        var result = calc.Add(2, 3);
        Assert.AreEqual(5, result);
    }
}
```

---

### **✅ Обработка зависимостей (Dependency Injection)**

**xUnit лучше поддерживает DI**, потому что использует конструктор для внедрения зависимостей:

📌 **xUnit (DI поддерживается)**

```csharp
public class UserServiceTests
{
    private readonly IUserRepository _repository;

    public UserServiceTests(IUserRepository repository)
    {
        _repository = repository;
    }

    [Fact]
    public void GetUser_ShouldReturnUser()
    {
        var user = _repository.GetUserById(1);
        Assert.NotNull(user);
    }
}
```

📌 **NUnit (DI не поддерживается, приходится использовать SetUp)**

```csharp
[TestFixture]
public class UserServiceTests
{
    private IUserRepository _repository;

    [SetUp]
    public void Setup()
    {
        _repository = new UserRepository();
    }

    [Test]
    public void GetUser_ShouldReturnUser()
    {
        var user = _repository.GetUserById(1);
        Assert.NotNull(user);
    }
}
```

💡 **Вывод:**

- В **xUnit** зависимости можно передавать в конструктор.
- В **NUnit** приходится использовать `[SetUp]`.

---

### **✅ Асинхронность (async/await)**

В **xUnit** поддержка асинхронности **из коробки**, тесты могут быть **асинхронными без необходимости использования `Task.Result` или `Task.Wait()`**. Можно просто объявить `async Task`, и xUnit корректно выполнит тест.
В **NUnit** с версии 3.0 появилась поддержка `async Task`. Однако в некоторых случаях могут возникнуть проблемы, например, если используется **неподдерживаемая версия NUnit или `void` вместо `Task`**.

📌 **xUnit (нормально работает с async/await)**

```csharp
[Fact]
public async Task GetData_ShouldReturnResult()
{
    var result = await _service.GetDataAsync();
    Assert.NotNull(result);
}
```

📌 **NUnit (нужно явно указывать async Task)**

```csharp
[Test]
public async Task GetData_ShouldReturnResult()
{
    var result = await _service.GetDataAsync();
    Assert.NotNull(result);
}
```

💡 **Вывод:** xUnit удобнее для асинхронных тестов.

---

### **✅ Группировка тестов (Test Collections vs Test Fixtures)**

В **xUnit** группировка тестов выполняется через `CollectionFixture`, а в NUnit — через `[TestFixture]`.

📌 **xUnit (CollectionFixture)**

```csharp
[CollectionDefinition("Database collection")]
public class DatabaseCollection : ICollectionFixture<DatabaseFixture> { }
```

📌 **NUnit (TestFixture)**

```csharp
[TestFixture]
public class DatabaseTests
{
    [Test]
    public void ConnectToDatabase_ShouldWork() { }
}
```

💡 **Вывод:**

- В **xUnit** фикстуры удобны для DI.
- В **NUnit** фикстуры более явные.

---

## **3️⃣ Когда использовать xUnit, NUnit и MSTest?**

### **🔹 Когда использовать xUnit?**

✅ Если проект новый и на .NET 6/7/8.  
✅ Если требуется поддержка Dependency Injection.  
✅ Если планируется CI/CD с GitHub Actions или Azure DevOps.  
✅ Если важна хорошая поддержка асинхронности.

🔥 **xUnit — это стандарт для .NET Core и .NET 6+**. Microsoft рекомендует **xUnit** для современных проектов.

---

### **🔹 Когда использовать NUnit?**

✅ Если проект старый и уже использует NUnit.  
✅ Если нужны `[TestFixture]`, `[TestCase]`, `[SetUp]`.  
✅ Если необходимо переиспользовать старые тесты с NUnit.

💡 **NUnit хорош, если он уже используется в проекте, но для новых проектов лучше выбрать xUnit.**

---

### **🔹 Когда использовать MSTest?**

✅ Если проект находится в экосистеме Microsoft (Azure, Visual Studio).  
✅ Если используется **Visual Studio Test Explorer**.  
✅ Если в компании принято использовать MSTest.

💡 **MSTest подходит для корпоративных решений, но менее гибок, чем xUnit и NUnit.**

---

## **🎯 Итоговое сравнение (Что выбрать?)**

|Фактор|xUnit|NUnit|MSTest|
|---|---|---|---|
|Рекомендуется Microsoft|✅ Да|❌ Нет|✅ Да|
|Подходит для новых проектов|✅ Да|🟡 Возможно|🟡 Возможно|
|Поддержка Dependency Injection|✅ Да|❌ Нет|✅ Да|
|Поддержка асинхронности|✅ Хорошо|🟡 Средне|✅ Хорошо|
|Используется в корпоративных проектах|🟡 Иногда|✅ Да|✅ Да|
|Поддержка Visual Studio|✅ Да|✅ Да|✅ Да|
|Простота настройки|✅ Легко|🟡 Средне|🟡 Средне|

🔹 **Выбор для новых проектов**: **xUnit**  
🔹 **Если используется NUnit — можно оставить**  
🔹 **Если проект в экосистеме Microsoft — MSTest**

---
## **Важное замечание: если тест слишком зависит от деталей - то ломается при любом изменени- это плохо.**

### 🔥 **Проблема: Слишком хрупкие тесты**

Если тесты сильно зависят от **внутренней реализации кода**, они начинают **ломаться при любом изменении**, даже если поведение системы осталось корректным.

### **Пример плохого теста (слишком привязан к реализации)**

```csharp
[Fact]
public void GetFullName_ShouldReturnConcatenatedString()
{
    var user = new User { FirstName = "John", LastName = "Doe" };
    var result = user.GetFullName();
    
    // Ожидаем, что строка будет строго в этом формате
    Assert.Equal("John Doe", result);
}
```

**Что здесь не так?**

- Если поменять способ объединения строк (`FirstName + " " + LastName` → `string.Join(" ", FirstName, LastName)`), тест сломается.
- Если добавится новая логика (например, `LastName` может быть `null`), тест снова сломается.
- Тест проверяет **как работает код**, а не **что он должен делать**.

---

### ✅ **Как исправить? (Писать тесты, проверяющие поведение, а не детали реализации)**

```csharp
[Fact]
public void GetFullName_ShouldContainFirstAndLastName()
{
    var user = new User { FirstName = "John", LastName = "Doe" };
    var result = user.GetFullName();
    
    // Проверяем ожидаемое поведение, а не точный формат
    Assert.Contains("John", result);
    Assert.Contains("Doe", result);
}
```

**Почему так лучше?**  
✅ Тест проверяет **бизнес-логику**, а не детали реализации.  
✅ Если в будущем формат строки изменится (`"Doe, John"` или `"John-Doe"`), тест не сломается, если поведение осталось корректным.  
✅ Меньше затрат на поддержку тестов при изменении кода.

---

### 🎯 **Вывод**

- Тесты должны **проверять поведение системы**, а не **как именно реализован код**.
- Изменение внутренней логики **не должно приводить к массовым сбоям тестов**, если система работает правильно.
- Хорошие тесты **гибкие и устойчивые**, а не хрупкие.

🚀 **Итог**: Если тест "ломается при любом изменении" — он плохо написан. Нужно тестировать **что делает код**, а не **как он это делает**.