### **1. Что такое Entities?**

Entities (Сущности) — это фундаментальный уровень, содержащий **бизнес-логику и основные модели данных**. Этот слой **не должен зависеть** от базы данных, UI, фреймворков и других внешних компонентов.

#### **Ключевые особенности сущностей:**

✔ Представляют бизнес-объекты, которые не зависят от деталей реализации.  
✔ Должны быть максимально чистыми и автономными.  
✔ Долговечны – изменения в базе данных или UI не должны их затрагивать.  
✔ Не содержат инфраструктурного кода (запросов в БД, HTTP-запросов и т. д.).

---

### **2. Примеры сущностей**

В качестве примера рассмотрим **сущность `Order` (Заказ)** в интернет-магазине.

```csharp
public class Order
{
    public int Id { get; private set; }
    public DateTime OrderDate { get; private set; }
    public decimal TotalAmount { get; private set; }
    public List<OrderItem> Items { get; private set; } = new();

    public Order(List<OrderItem> items)
    {
        if (items == null || !items.Any())
            throw new ArgumentException("Заказ должен содержать хотя бы один товар");

        Items = items;
        OrderDate = DateTime.UtcNow;
        TotalAmount = items.Sum(i => i.Price * i.Quantity);
    }

    public void AddItem(OrderItem item)
    {
        if (item == null) throw new ArgumentException("Товар не может быть null");

        Items.Add(item);
        TotalAmount += item.Price * item.Quantity;
    }
}

public class OrderItem
{
    public int ProductId { get; private set; }
    public int Quantity { get; private set; }
    public decimal Price { get; private set; }

    public OrderItem(int productId, int quantity, decimal price)
    {
        if (quantity <= 0) throw new ArgumentException("Количество должно быть больше 0");
        if (price <= 0) throw new ArgumentException("Цена должна быть больше 0");

        ProductId = productId;
        Quantity = quantity;
        Price = price;
    }
}
```

📌 **Важно**:

- Сущности инкапсулируют данные и поведение.
- Они не должны "знать" о базах данных, API, UI.

---

### **3. Отличие от DTO и моделей базы данных**

|**Характеристика**|**Entities (Сущности)**|**DTO (Data Transfer Object)**|**Модели БД**|
|---|---|---|---|
|Цель|Представляет бизнес-логику|Переносит данные между слоями|Представляет структуру таблицы|
|Содержит бизнес-логику?|✅ Да|❌ Нет|❌ Нет|
|Зависит от базы данных?|❌ Нет|❌ Нет|✅ Да|
|Используется в слое|Entities (Core)|Application (Use Cases)|Infrastructure (EF Core)|

---

### **4. Связь с остальными слоями**

- **Use Cases** (сценарии использования) используют **Entities** для выполнения бизнес-логики.
- **Infrastructure** (БД) может преобразовывать данные в **Entities**, но не наоборот.
- **Presentation (UI, API)** не работает с сущностями напрямую — вместо этого используется **DTO**.

---

### **5. Преимущества использования сущностей**

✅ **Изоляция бизнес-логики** (не зависит от деталей реализации).  
✅ **Переиспользуемость** (можно использовать в разных приложениях).  
✅ **Тестируемость** (легко покрывать Unit-тестами).

---

### **6. Когда не нужны сущности?**

❌ Если приложение **чисто CRUD** (например, простая админ-панель).  
❌ Если нет сложной логики (просто храним и выдаем данные).

---
