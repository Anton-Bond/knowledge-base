### **1. Что такое Use Cases?**

Use Cases (Сценарии использования) — это **уровень бизнес-логики**, который описывает, как система должна работать в рамках конкретных операций. Этот слой содержит **сервисы и обработчики команд**, которые **используют сущности (Entities)**, но не зависят от инфраструктуры (БД, API, UI).

#### **Ключевые особенности Use Cases**:

✔ Инкапсулируют **бизнес-правила** и **алгоритмы**.  
✔ Работают **только с сущностями (Entities)**.  
✔ Не содержат SQL-запросов, HTTP-запросов, кода базы данных.  
✔ Используются в сервисах, обработчиках команд, интеракторах.

---

### **2. Пример Use Case**

Рассмотрим **сценарий оформления заказа** в интернет-магазине.

📌 **Требования:**

- Заказ можно создать только при наличии товаров.
- Сервис должен работать **независимо** от базы данных.
- При создании заказа сумма должна рассчитываться автоматически.

```csharp
public class CreateOrderUseCase
{
    public Order Execute(List<OrderItem> items)
    {
        if (items == null || !items.Any())
            throw new ArgumentException("Заказ должен содержать хотя бы один товар");

        var order = new Order(items);
        return order;
    }
}
```

📌 **Здесь:**

- `CreateOrderUseCase` — **сценарий** оформления заказа.
- Он **не зависит от БД**, UI, API, фреймворков.
- Вся логика сосредоточена в **сущности `Order`**.

---

### **3. Связь с другими слоями**

🔹 **Entities (Сущности)** – Use Case использует их, но не изменяет их внутреннюю структуру.  
🔹 **Infrastructure (Инфраструктура)** – передает данные в БД, но не влияет на логику Use Case.  
🔹 **Presentation (UI, API)** – вызывает Use Case, но не реализует в нем свою логику.

📌 **Как это работает?**

1. Контроллер принимает HTTP-запрос.
2. Контроллер вызывает `CreateOrderUseCase`.
3. Use Case выполняет бизнес-логику.
4. Контроллер передает результат клиенту.

```csharp
[ApiController]
[Route("api/orders")]
public class OrderController : ControllerBase
{
    private readonly CreateOrderUseCase _createOrderUseCase;

    public OrderController(CreateOrderUseCase createOrderUseCase)
    {
        _createOrderUseCase = createOrderUseCase;
    }

    [HttpPost]
    public IActionResult CreateOrder([FromBody] List<OrderItem> items)
    {
        var order = _createOrderUseCase.Execute(items);
        return Ok(order);
    }
}
```

---

### **4. Отличие Use Cases от Сущностей**

|**Характеристика**|**Entities (Сущности)**|**Use Cases (Сценарии)**|
|---|---|---|
|Содержит бизнес-логику?|✅ Да|✅ Да (но более высокоуровневую)|
|Взаимодействует с UI?|❌ Нет|❌ Нет|
|Использует БД?|❌ Нет|❌ Нет|
|Использует другие сервисы?|❌ Нет|✅ Да (через интерфейсы)|

---

### **5. Когда нужен Use Case слой?**

✅ Если есть **сложная бизнес-логика**, которую нельзя разместить в сущностях.  
✅ Если требуется **разделить обработку данных** между слоями.  
✅ Если **важна тестируемость** (Use Cases легко тестировать без БД).

---

### **6. Когда можно обойтись без Use Cases?**

❌ Если приложение **CRUD без логики** (простое чтение/запись данных).  
❌ Если сущности **уже достаточно автономны** (Use Case просто вызовет метод сущности).

---

### **7. Преимущества Use Cases**

✅ **Чистая логика** – отделение от инфраструктуры.  
✅ **Гибкость** – можно менять реализацию без изменения UI или БД.  
✅ **Легко тестировать** – можно проверять бизнес-правила отдельно.

---
