### **1. Что такое Presentation?**

Слой **Presentation** — это слой, который отвечает за **взаимодействие с пользователем** или **взаимодействие с внешними клиентами через API**. Этот слой **представляет данные в удобной форме** для пользователя (например, через UI) или для клиента (через API). Он **не должен содержать бизнес-логики** и должен зависеть от более высоких слоев, таких как **Use Cases**.

#### **Ключевые особенности Presentation:**

✔ Отвечает за **вывод данных** (через UI, API).  
✔ Использует **DTO (Data Transfer Objects)** для передачи данных между слоями.  
✔ Обрабатывает **входные запросы** от клиента (например, обработка HTTP-запросов).  
✔ Должен быть **независим от бизнес-логики** и инфраструктуры (работает через Use Cases и интерфейсы).

---

### **2. Пример: Контроллер API в слое Presentation**

Рассмотрим пример, где мы предоставляем API для создания и получения заказов. Контроллер будет обращаться к **Use Cases** для выполнения операций, а также будет использовать **DTO** для передачи данных между слоями.

```csharp
// DTO для передачи данных о заказе
public class OrderDTO
{
    public int Id { get; set; }
    public DateTime OrderDate { get; set; }
    public decimal TotalAmount { get; set; }
    public List<OrderItemDTO> Items { get; set; }
}

// DTO для передачи данных о товаре в заказе
public class OrderItemDTO
{
    public int ProductId { get; set; }
    public int Quantity { get; set; }
    public decimal Price { get; set; }
}

// Контроллер API
[ApiController]
[Route("api/orders")]
public class OrderController : ControllerBase
{
    private readonly CreateOrderUseCase _createOrderUseCase;
    private readonly IOrderRepository _orderRepository;

    public OrderController(CreateOrderUseCase createOrderUseCase, IOrderRepository orderRepository)
    {
        _createOrderUseCase = createOrderUseCase;
        _orderRepository = orderRepository;
    }

    // Создание нового заказа
    [HttpPost]
    public async Task<IActionResult> CreateOrder([FromBody] List<OrderItemDTO> items)
    {
        // Преобразуем DTO в сущности
        var orderItems = items.Select(i => new OrderItem(i.ProductId, i.Quantity, i.Price)).ToList();
        var order = _createOrderUseCase.Execute(orderItems);

        // Сохраняем заказ в БД через репозиторий
        await _orderRepository.AddAsync(order);

        // Преобразуем сущность обратно в DTO
        var orderDto = new OrderDTO
        {
            Id = order.Id,
            OrderDate = order.OrderDate,
            TotalAmount = order.TotalAmount,
            Items = order.Items.Select(i => new OrderItemDTO { ProductId = i.ProductId, Quantity = i.Quantity, Price = i.Price }).ToList()
        };

        return CreatedAtAction(nameof(GetOrderById), new { id = orderDto.Id }, orderDto);
    }

    // Получение заказа по ID
    [HttpGet("{id}")]
    public async Task<IActionResult> GetOrderById(int id)
    {
        var order = await _orderRepository.GetByIdAsync(id);
        if (order == null) return NotFound();

        var orderDto = new OrderDTO
        {
            Id = order.Id,
            OrderDate = order.OrderDate,
            TotalAmount = order.TotalAmount,
            Items = order.Items.Select(i => new OrderItemDTO { ProductId = i.ProductId, Quantity = i.Quantity, Price = i.Price }).ToList()
        };

        return Ok(orderDto);
    }
}
```

📌 **Здесь:**

- Контроллер **взаимодействует с Use Cases** для обработки бизнес-логики.
- Контроллер **использует DTO**, чтобы передать данные между слоями (представление данных для клиента).
- **Нет бизнес-логики** в контроллере — вся логика находится в Use Cases и Entities.

---

### **3. Связь с другими слоями**

🔹 **Use Cases (Сценарии)** — контроллеры в Presentation используют Use Cases для выполнения операций.  
🔹 **Entities (Сущности)** — данные сущностей передаются в Presentation через **DTO**, которые являются абстракцией для представления данных.  
🔹 **Infrastructure (Инфраструктура)** — предоставляют данные для Use Cases, но Presentation не взаимодействует напрямую с этим слоем.

📌 **Как это работает?**

1. В UI или API клиент делает запрос (например, POST для создания заказа).
2. Контроллер передает запрос в **Use Case** для обработки логики.
3. Use Case работает с **Entities** для выполнения операции.
4. Контроллер возвращает результат клиенту (например, заказ в формате JSON).

---

### **4. Отличие Presentation от других слоев**

|**Характеристика**|**Entities (Сущности)**|**Use Cases (Сценарии)**|**Presentation**|
|---|---|---|---|
|Содержит бизнес-логику?|✅ Да|✅ Да|❌ Нет|
|Зависит от других слоев?|❌ Нет|✅ Да (от Use Cases)|✅ Да (от Use Cases)|
|Взаимодействует с БД?|❌ Нет|❌ Нет|❌ Нет|
|Работает с пользовательским вводом?|❌ Нет|❌ Нет|✅ Да (UI или API)|

---

### **5. Когда нужен слой Presentation?**

✅ Когда приложение **работает с пользователем** через UI или API.  
✅ Если требуется **представить данные в удобном виде** для пользователя (например, через веб-страницу, мобильное приложение, или через API).  
✅ Если приложение должно быть **гибким в отношении UI** (переход к REST API или MVC в будущем).

---

### **6. Когда можно обойтись без слоя Presentation?**

❌ Если приложение не имеет **взаимодействия с пользователем** или клиентами (например, это сервис с фоновыми задачами).  
❌ Если приложение использует только **CLI** и не требует полноценного UI/API.

---

### **7. Преимущества Presentation слоя**

✅ **Отделение логики от UI** — бизнес-логика остается в Use Cases и Entities.  
✅ **Чистота кода** — контроллеры и представление не загромождаются бизнес-логикой.  
✅ **Гибкость и расширяемость** — можно легко менять UI или API без затрагивания бизнес-логики.

---
