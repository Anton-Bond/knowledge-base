### **1. Что такое Infrastructure?**

Инфраструктура (Infrastructure) — это слой, который отвечает за **взаимодействие с внешними компонентами системы**, такими как базы данных, сторонние API, сервисы, библиотеки и другие технологии. Этот слой **не должен содержать бизнес-логику**, а служит для **реализации взаимодействия с внешним миром**.

#### **Ключевые особенности инфраструктуры:**

✔ Реализует **подключение к базе данных**, внешним сервисам, библиотекам.  
✔ Содержит **репозитории**, **сервисы для интеграции** с внешними API и другие компоненты.  
✔ Этот слой **зависит от более высоких слоев**, но не наоборот (например, зависит от **Use Cases**).  
✔ Здесь могут быть реализованы механизмы работы с кешированием, очередями, внешними сервисами и т. д.

---

### **2. Пример: Инфраструктурный слой с репозиторием**

Предположим, что у нас есть **репозиторий для работы с заказами** в базе данных. Репозиторий реализует интерфейс, который определяет методы взаимодействия с данными.

```csharp
// Интерфейс репозитория, определяющий контракт
public interface IOrderRepository
{
    Task<Order> GetByIdAsync(int orderId);
    Task<IEnumerable<Order>> GetAllAsync();
    Task AddAsync(Order order);
}
```

Реализация репозитория для работы с базой данных с использованием **Entity Framework Core**:

```csharp
public class OrderRepository : IOrderRepository
{
    private readonly DbContext _context;

    public OrderRepository(DbContext context)
    {
        _context = context;
    }

    public async Task<Order> GetByIdAsync(int orderId)
    {
        return await _context.Set<Order>().FindAsync(orderId);
    }

    public async Task<IEnumerable<Order>> GetAllAsync()
    {
        return await _context.Set<Order>().ToListAsync();
    }

    public async Task AddAsync(Order order)
    {
        await _context.Set<Order>().AddAsync(order);
        await _context.SaveChangesAsync();
    }
}
```

📌 **Обратите внимание:**

- `OrderRepository` зависит от базы данных (через `DbContext`), но бизнес-логика находится в слое **Use Cases** и **Entities**.
- **Инфраструктурный слой** предоставляет реализацию для взаимодействия с внешними сервисами, но не решает, как именно обрабатываются данные.

---

### **3. Связь с другими слоями**

🔹 **Use Cases (Сценарии)** — инфраструктурный слой реализует интерфейсы, которые используют Use Cases для доступа к данным.  
🔹 **Entities (Сущности)** — взаимодействуют с инфраструктурой через репозитории и сервисы, но сущности **не содержат** инфраструктурной логики.  
🔹 **Presentation (UI, API)** — использует Use Cases, которые в свою очередь могут использовать инфраструктуру для получения данных.

📌 **Как это работает?**

1. Контроллер вызывает Use Case.
2. Use Case использует репозитории или внешние сервисы из инфраструктуры для получения/сохранения данных.
3. Ответ от Use Case возвращается в контроллер для отображения пользователю.

```csharp
public class OrderController : ControllerBase
{
    private readonly IOrderRepository _orderRepository;

    public OrderController(IOrderRepository orderRepository)
    {
        _orderRepository = orderRepository;
    }

    [HttpGet("{id}")]
    public async Task<IActionResult> GetOrderById(int id)
    {
        var order = await _orderRepository.GetByIdAsync(id);
        if (order == null) return NotFound();
        return Ok(order);
    }

    [HttpPost]
    public async Task<IActionResult> CreateOrder([FromBody] List<OrderItem> items)
    {
        var order = new Order(items);
        await _orderRepository.AddAsync(order);
        return CreatedAtAction(nameof(GetOrderById), new { id = order.Id }, order);
    }
}
```

---

### **4. Отличие Infrastructure от других слоев**

|**Характеристика**|**Entities (Сущности)**|**Use Cases (Сценарии)**|**Infrastructure**|
|---|---|---|---|
|Содержит бизнес-логику?|✅ Да|✅ Да|❌ Нет|
|Зависит от других слоев?|❌ Нет|✅ Да (от Use Cases)|✅ Да (от Use Cases)|
|Взаимодействует с базой данных?|❌ Нет|❌ Нет|✅ Да|
|Взаимодействует с внешними сервисами?|❌ Нет|❌ Нет|✅ Да|

---

### **5. Когда нужен инфраструктурный слой?**

✅ Когда требуется взаимодействие с **внешними ресурсами** (базы данных, API, очереди и т. д.).  
✅ Когда необходимо **сохранение и извлечение данных** из базы данных или внешних сервисов.  
✅ Если приложение должно быть **независимым от реализации** конкретных технологий (например, можно легко поменять базу данных).

---

### **6. Когда можно обойтись без инфраструктуры?**

❌ Если приложение **не взаимодействует с внешними системами**.  
❌ Если приложение **не использует постоянное хранилище данных** (например, только сессионные данные).

---

### **7. Преимущества инфраструктурного слоя**

✅ **Разделение ответственности** – бизнес-логика не связана с деталями реализации внешних сервисов.  
✅ **Масштабируемость** – можно легко поменять реализацию инфраструктуры без изменений в других слоях.  
✅ **Тестируемость** – позволяет легко замещать зависимости для тестирования.

---
