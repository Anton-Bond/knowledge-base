**Коллекция уникальных элементов.**

**Особенности:**
- Не допускаются дублирующиеся элементы.
- Используется класс `HashSet<T>`.

```C#
HashSet<int> set = new HashSet<int> { 1, 2, 3 };
set.Add(2); // Игнорируется, так как 2 уже есть

```