
# Полный, структурированный и удобный справочник по Python окружениям, pip, requirements и VS Code в WSL

## 🐍 1. Виртуальные окружения (venv)

### ✔ Создать виртуальное окружение

```bash
python3 -m venv .venv
```

### ✔ Активировать

```bash
source .venv/bin/activate
```

### ✔ Деактивировать

```bash
deactivate
```

### ✔ Удалить окружение

(просто удалить папку)

```bash
rm -rf .venv
```

---

## 📦 2. pip — управление пакетами

### ✔ Установить пакет

```bash
pip install requests
```

### ✔ Установить несколько пакетов

```bash
pip install fastapi uvicorn pydantic
```

### ✔ Установить из файла requirements.txt

```bash
pip install -r requirements.txt
```

### ✔ Удалить пакет

```bash
pip uninstall requests
```

### ✔ Показать установленные пакеты

```bash
pip list
```

### ✔ Найти пакет

```bash
pip search fastapi
```

(может быть отключено по политике PyPI)

---

## 📄 3. Работа с requirements.txt

### ✔ Заморозить зависимости

Записывает текущие пакеты в файл:

```bash
pip freeze > requirements.txt
```

### ✔ Установить пакеты из файла

```bash
pip install -r requirements.txt
```

---

## 🗂 4. Структура типового Python проекта

```output
project/
│
├── .venv/               # виртуальное окружение
├── .vscode/
│   └── settings.json    # настройки VS Code
│
├── main.py              # основной файл
├── requirements.txt     # зависимости
└── README.md
```

---

## 🟦 5. VS Code + WSL

### ✔ Открыть проект из WSL

```bash
code .
```

### ✔ Настройки Python в VS Code

Создай файл `.vscode/settings.json`:

```json
{
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
    "python.terminal.activateEnvironment": true,
    "python.analysis.autoImportCompletions": true,
    "python.linting.enabled": true
}
```

VS Code автоматически:

* активирует виртуальное окружение в терминале
* использует правильный Python
* показывает autocompletion для зависимостей

---

## 🧪 6. pytest (если нужен для тестов)

### ✔ Установка

```bash
pip install pytest
```

### ✔ Запуск тестов

```bash
pytest
```

---

## 🧹 7. Форматирование кода (опционально)

Рекомендация: использовать **Black + isort + Flake8**

### Установка

```bash
pip install black flake8 isort
```

### Форматирование

```bash
black .
isort .
flake8
```

---

## 📌 8. Несколько полезных команд

### ✔ Показать путь до Python

```bash
which python
```

Ожидаем внутри окружения:

```bash
/home/user/workspace/project/.venv/bin/python
```

### ✔ Проверить версию pip

```bash
pip -V
```

### ✔ Обновить pip

```bash
pip install --upgrade pip
```

---

## ⚡ 9. Короткие FAQ

### ❓ Можно ли хранить venv в другой папке?

Да, но для VS Code удобнее, когда окружение называется `.venv` в корне проекта.

### ❓ Можно ли иметь несколько окружений одновременно?

Да, хоть 20 — они изолированы.

### ❓ В VS Code Python неправильный?

Открой Command Palette → **Python: Select Interpreter**

---
