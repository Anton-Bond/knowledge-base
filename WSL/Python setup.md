# Инструкция как настроить Python под WSL (Ubuntu 24.04) и работать с проектами через VS Code на Windows 11

Инструкция включает:

✔ установка Python
✔ создание изолированных виртуальных окружений
✔ настройка VS Code + WSL
✔ автоматическая активация окружения
✔ проверка, что всё работает

---

## ✅ 1. Проверяем Python в WSL

Открой **Ubuntu WSL**:

```bash
python3 --version
```

Если Python есть — отлично. В Ubuntu 24.04 обычно стоит Python 3.12.

Чтобы установить на всякий случай:

```bash
sudo apt update
sudo apt install python3 python3-venv python3-pip -y
```

---

## ✅ 2. Создание рабочей папки для проектов

Делать надо **внутри WSL**, а не на диске C:.

```bash
mkdir -p ~/workspace
cd ~/workspace
```

---

## ✅ 3. Создать проект и виртуальное окружение

Допустим, проект называется `projectC`:

```bash
mkdir projectC
cd projectC
```

Создаём виртуальное окружение:

```bash
python3 -m venv .venv
```

Активируем:

```bash
source .venv/bin/activate
```

Проверяем:

```bash
python -V
pip -V
```

---

## 🔁 4. Автоактивация виртуального окружения в VS Code

VS Code сам понимает `.venv`, если папка называется точно `.venv`.

Чтобы VS Code всегда использовал правильный интерпретатор, в проекте создаётся файл:

**`.vscode/settings.json`**

VS Code делает это автоматически, но можно создать вручную:

```bash
mkdir -p .vscode
nano .vscode/settings.json
```

Вставить:

```json
{
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
    "python.terminal.activateEnvironment": true,
    "python.analysis.autoImportCompletions": true,
    "python.linting.enabled": true
}
```

VS Code автоматически:

* активирует виртуальное окружение в терминале
* использует правильный Python
* показывает autocompletion для зависимостей

Сохрани `Ctrl+O` → `Enter` → `Ctrl+X`.

---

## 🟣 5. Открыть проект в VS Code через WSL (правильный способ)

Никогда не открывай WSL проект обычным Windows VS Code.

Правильно:

В WSL-терминале:

```bash
cd ~/workspace/projectC
code .
```

VS Code откроется в режиме **Remote - WSL** (это важно!).

---

## 🟢 6. Установить Python расширение в VS Code

VS Code сам предложит установить.

Если нет — установи вручную:

1. Нажми на Extensions (Ctrl+Shift+X)
2. Найди **Python** (Microsoft)
3. Установи
4. VS Code автоматически подтягивает интерпретатор из `.venv`

---

## 🧪 7. Проверка — работает ли окружение

Создай файл:

**`main.py`**

```python
import sys

print("Python executable:", sys.executable)
print("Python version:", sys.version)
```

Запусти:

```bash
python main.py
```

Ожидаемый вывод:

```bash
Python executable: /home/username/workspace/projectC/.venv/bin/python
Python version: 3.xx ...
```

Если так — всё работает.

---

## 🧰 8. Установка зависимостей

Внутри активированного venv:

```bash
pip install requests
pip install fastapi
```

Список зависимостей:

```bash
pip freeze > requirements.txt
```

---

## ⚡ 9. Работа с несколькими Python-проектами

Для каждого проекта:

✔ отдельная папка
✔ отдельный `.venv`
✔ VS Code сам переключается

То есть:

```text
project1/.venv
project2/.venv
project3/.venv
```

---

## 🔐 10. (Опционально) Ускорить Python на WSL

Включить пакет dev:

```bash
sudo apt install python3-dev build-essential -y
```

Это нужно для библиотек, которым нужно собирать C-модули.

---
---

## ✅ Как деактивировать виртуальное окружение Python

Выполни в терминале:

```bash
deactivate
```

После этого:

* пропадает префикс `(.venv)` в начале строки
* Python и pip снова будут глобальными системными

---

## 📌 Если deactivate не работает (редко)

Иногда может помочь:

```bash
.venv/bin/deactivate
```

или:

```bash
source .venv/bin/deactivate
```

Но в 99% случаев достаточно просто:

```bash
deactivate
```

---
